---
phase: 05-tests-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_api.py
  - tests/test_config_router.py
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "POST /process con excepcion generica retorna 500 con error internal_error"
    - "POST /config con body no-JSON retorna 422 con error invalid_json"
    - "POST /config con cambio de modelo dispara _swap_rembg_session"
    - "Suite completa de tests pasa en verde con cobertura >= 80%"
  artifacts:
    - path: "tests/test_api.py"
      provides: "Test de 500 internal error en POST /process"
      contains: "test_process_500_internal_error"
    - path: "tests/test_config_router.py"
      provides: "Test de invalid JSON en POST /config"
      contains: "test_post_config_invalid_json_body"
    - path: "tests/test_config_router.py"
      provides: "Test de model change trigger en POST /config"
      contains: "test_post_config_model_change"
  key_links:
    - from: "tests/test_api.py"
      to: "app/router_api.py"
      via: "httpx AsyncClient POST /process"
      pattern: "client\\.post.*process"
    - from: "tests/test_config_router.py"
      to: "app/router_config.py"
      via: "httpx AsyncClient POST /config"
      pattern: "client\\.post.*config"
---

<objective>
Completar los gaps de tests de API (TEST-03) agregando 3 tests nuevos: error 500 en POST /process, invalid JSON en POST /config, y model change trigger en POST /config. Estos cubren las lineas faltantes de router_api.py (150-157) y router_config.py (55-56, 117-118).

Purpose: Verificar el comportamiento defensivo de la API bajo condiciones de error que las fases anteriores no testearon.
Output: test_api.py y test_config_router.py actualizados con 3 tests nuevos, todos pasando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests-hardening/05-CONTEXT.md
@.planning/phases/05-tests-hardening/05-RESEARCH.md

<interfaces>
<!-- Patrones de test establecidos que el executor debe seguir -->

From tests/test_api.py (patron de fixtures):
```python
@pytest.fixture
async def client_with_queue():
    mock_session = MagicMock()
    with patch("app.main.new_session", return_value=mock_session):
        async with app.router.lifespan_context(app):
            async with AsyncClient(
                transport=ASGITransport(app=app),
                base_url="http://test",
            ) as client:
                queue = app.state.job_queue
                yield client, queue
```

From tests/test_config_router.py (patron de app de test aislada):
```python
@pytest.fixture
async def test_app(tmp_path):
    yaml_path = tmp_path / "settings.yaml"
    # ... crea settings.yaml ...
    app = FastAPI(lifespan=_mock_lifespan)
    app.include_router(config_router)
    cm = ConfigManager(config_path=str(yaml_path))
    app.state.config_manager = cm
    app.state.job_queue = JobQueue(...)
    app.state.model_name = "isnet-general-use"
    # ... yield app
```

From app/router_api.py (lineas 150-157 — catch generico):
```python
except Exception as e:
    return JSONResponse(
        status_code=500,
        content=ErrorResponse(
            error="internal_error",
            detail="Internal server error",
            article_id=article_id,
        ).model_dump(),
    )
```

From app/router_config.py (lineas 54-59 — invalid JSON):
```python
try:
    body = await request.json()
except Exception as e:
    return JSONResponse(
        status_code=422,
        content={"error": "invalid_json", "detail": str(e)},
    )
```

From app/router_config.py (lineas 116-118 — model change):
```python
if model_changed:
    from app.main import _swap_rembg_session
    asyncio.create_task(_swap_rembg_session(request.app, new_model))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar test de 500 internal error en POST /process (TEST-03)</name>
  <files>tests/test_api.py</files>
  <read_first>
    - tests/test_api.py
    - tests/conftest.py
    - app/router_api.py
  </read_first>
  <action>
Agregar al final de tests/test_api.py el siguiente test. Per D-06 y D-07: mockear submit_job (no process_image) y verificar respuesta 500.

```python
async def test_process_500_internal_error(client_with_queue):
    """POST /process con excepcion generica retorna 500 con error internal_error."""
    client, queue = client_with_queue

    # Crear imagen JPEG valida para el request
    buf = io.BytesIO()
    img = Image.new("RGB", (100, 100), (255, 0, 0))
    img.save(buf, format="JPEG")
    jpeg_bytes = buf.getvalue()

    with patch.object(
        queue,
        "submit_job",
        new=AsyncMock(side_effect=RuntimeError("unexpected failure")),
    ):
        response = await client.post(
            "/process",
            files={"image": ("test.jpg", jpeg_bytes, "image/jpeg")},
            data={"article_id": "ERR-001"},
        )
    assert response.status_code == 500
    body = response.json()
    assert body["error"] == "internal_error"
    assert body["article_id"] == "ERR-001"
```

Verificar que los imports necesarios ya estan en el archivo. Si falta alguno de estos, agregarlo:
- `import io`
- `from PIL import Image`
- `from unittest.mock import patch, MagicMock, AsyncMock`

NOTA: No agregar `@pytest.mark.asyncio` — el proyecto usa `asyncio_mode = "auto"` en pyproject.toml (todas las funciones async se detectan automaticamente).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_api.py -k "test_process_500" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_process_500_internal_error" tests/test_api.py` returns 1
    - `grep "internal_error" tests/test_api.py` matches the assertion line
    - `.venv/bin/python -m pytest tests/test_api.py -k "test_process_500" -x -q` exits 0
  </acceptance_criteria>
  <done>POST /process 500 internal error cubierto. router_api.py lineas 150-157 ya no estan en "missing" de coverage</done>
</task>

<task type="auto">
  <name>Task 2: Agregar tests de config router gaps + verificacion final de cobertura (TEST-03)</name>
  <files>tests/test_config_router.py</files>
  <read_first>
    - tests/test_config_router.py
    - app/router_config.py
    - app/config.py
  </read_first>
  <action>
Agregar al final de tests/test_config_router.py los siguientes 2 tests. Per D-06: verificar invalid JSON y model change trigger.

**Test 1: test_post_config_invalid_json_body** (cubre router_config.py lineas 55-56)
```python
async def test_post_config_invalid_json_body(test_app):
    """POST /config con body que no es JSON retorna 422 con error invalid_json."""
    async with AsyncClient(
        transport=ASGITransport(app=test_app), base_url="http://test"
    ) as client:
        response = await client.post(
            "/config",
            content=b"this is not json at all",
            headers={"Content-Type": "application/json"},
        )
    assert response.status_code == 422
    body = response.json()
    assert body["error"] == "invalid_json"
```

**Test 2: test_post_config_model_change** (cubre router_config.py lineas 117-118)
```python
async def test_post_config_model_change(test_app):
    """POST /config con cambio de modelo dispara _swap_rembg_session via create_task."""
    async with AsyncClient(
        transport=ASGITransport(app=test_app), base_url="http://test"
    ) as client:
        with patch("app.router_config._swap_rembg_session", new=AsyncMock()) as mock_swap:
            response = await client.post(
                "/config",
                json={"rembg": {"model": "u2net"}},
            )
    assert response.status_code == 200
    body = response.json()
    assert body["rembg"]["model"] == "u2net"
```

NOTA sobre el test de model_change: el import de `_swap_rembg_session` en router_config.py es un import local (`from app.main import _swap_rembg_session`). Hay que patchearlo donde se usa. Si el patch en "app.router_config._swap_rembg_session" no funciona porque es un import local dentro de la funcion, usar en cambio:
```python
with patch("app.main._swap_rembg_session", new=AsyncMock()) as mock_swap:
```
Y verificar que la respuesta contiene el modelo actualizado. La linea 117-118 se cubre si `model_changed` es True, lo cual ocurre cuando el modelo en el body difiere del `app.state.model_name` actual ("isnet-general-use" en la fixture).

Verificar que los imports necesarios ya estan. Si falta `AsyncMock`, agregar:
```python
from unittest.mock import AsyncMock
```

Luego de agregar los tests, correr la suite completa con coverage:
```bash
.venv/bin/python -m pytest tests/ --cov=app --cov-report=term-missing -q
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_config_router.py -k "invalid_json or model_change" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_post_config_invalid_json_body" tests/test_config_router.py` returns 1
    - `grep -c "test_post_config_model_change" tests/test_config_router.py` returns 1
    - `.venv/bin/python -m pytest tests/test_config_router.py -k "invalid_json or model_change" -x -q` exits 0
    - `.venv/bin/python -m pytest tests/ --cov=app -q` exits 0 with overall coverage >= 80%
  </acceptance_criteria>
  <done>3 tests nuevos de API pasando en verde. Suite completa >= 80% cobertura. TEST-01, TEST-02, TEST-03 todos satisfechos</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/ --cov=app --cov-report=term-missing -q` muestra:
  - Todos los tests pasan (98+ tests)
  - router_api.py: lineas 150-157 ya cubiertas
  - router_config.py: lineas 55-56, 117-118 ya cubiertas
  - Cobertura total >= 80% (partimos de 93%, deberia subir a ~95%+)
</verification>

<success_criteria>
- 3 tests nuevos (1 en test_api.py, 2 en test_config_router.py) pasando en verde
- Suite completa pasa con cobertura >= 80%
- router_api.py y router_config.py con gaps cubiertos
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests-hardening/05-02-SUMMARY.md`
</output>
