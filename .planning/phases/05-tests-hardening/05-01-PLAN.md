---
phase: 05-tests-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_processor.py
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "autocrop maneja imagen completamente transparente sin crash (bbox=None)"
    - "_clean_alpha_artifacts elimina regiones pequenas y preserva la region principal"
    - "Excepcion generica en pipeline se envuelve en ProcessingError(step=unknown)"
    - "Pipeline con brightness=1.0 y contrast=1.0 no incluye enhance en steps_applied"
    - "Queue tests existentes siguen en verde (100% cobertura)"
  artifacts:
    - path: "tests/test_processor.py"
      provides: "Tests de gaps del processor"
      contains: "test_autocrop_bbox_none"
    - path: "tests/test_processor.py"
      provides: "Tests de gaps del processor"
      contains: "test_autocrop_removes_small_artifacts"
    - path: "tests/test_processor.py"
      provides: "Tests de gaps del processor"
      contains: "test_pipeline_unknown_exception_wrapped"
    - path: "tests/test_processor.py"
      provides: "Tests de gaps del processor"
      contains: "test_process_image_enhance_not_in_steps_when_default"
  key_links:
    - from: "tests/test_processor.py"
      to: "app/processor.py"
      via: "import autocrop, process_image, ProcessingError"
      pattern: "from app\\.processor import"
---

<objective>
Completar los gaps de tests del processor (TEST-01) agregando 4 tests nuevos a test_processor.py que cubren las 18 lineas faltantes de processor.py. Instalar pytest-cov en el venv. Verificar que los tests de queue (TEST-02) siguen en verde con 100% cobertura.

Purpose: Cubrir edge cases criticos del pipeline que las fases anteriores implementaron pero no testearon exhaustivamente: alpha artifacts multi-componente, autocrop sin bbox, exception wrapping, y enhance skip.
Output: test_processor.py actualizado con 4 tests nuevos, todos pasando en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests-hardening/05-CONTEXT.md
@.planning/phases/05-tests-hardening/05-RESEARCH.md

<interfaces>
<!-- Funciones del processor que se testean — firmas y comportamiento -->

From app/processor.py:
```python
def _clean_alpha_artifacts(alpha: Image.Image, min_ratio: float = 0.01) -> Image.Image:
    """Elimina regiones opacas pequenas del canal alpha. Retorna alpha sin cambios si arr.max()==0 o num_features<=1."""

def autocrop(img: Image.Image, config: AppConfig) -> Image.Image:
    """Recorta al bounding box. Retorna img sin cambios si bbox is None o area<5%."""

def process_image(image_bytes: bytes, article_id: str, config: AppConfig, rembg_session) -> ProcessingResult:
    """Pipeline completo. Raises ProcessingError. Wraps unknown exceptions in ProcessingError(step='unknown')."""
    # enhance se llama siempre pero solo se agrega a steps_applied si brightness!=1.0 or contrast!=1.0
```

From app/models.py:
```python
class ProcessingError(Exception):
    def __init__(self, step: str, detail: str = ""):
        self.step = step
        self.detail = detail

class AppConfig(BaseModel):
    # enhancement.brightness: float = 1.0
    # enhancement.contrast: float = 1.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar pytest-cov y verificar suite existente</name>
  <files>requirements-dev.txt</files>
  <read_first>
    - requirements-dev.txt
    - pyproject.toml
  </read_first>
  <action>
1. Instalar pytest-cov en el venv:
   ```bash
   .venv/bin/pip install "pytest-cov>=6.0"
   ```

2. Verificar que pytest-cov es importable:
   ```bash
   .venv/bin/python -c "import pytest_cov; print(pytest_cov.__version__)"
   ```

3. Verificar que los 94 tests existentes siguen pasando:
   ```bash
   .venv/bin/python -m pytest tests/ -x -q
   ```

4. Correr coverage baseline para confirmar el estado inicial:
   ```bash
   .venv/bin/python -m pytest tests/ --cov=app --cov-report=term-missing -q
   ```

5. Verificar que pytest-cov ya esta en requirements-dev.txt. Si no esta, agregar la linea `pytest-cov>=6.0` al archivo.

Per D-02: coverage target es 80%+ medido con pytest --cov=app --cov-report=term-missing.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ --cov=app -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import pytest_cov"` exits 0
    - `.venv/bin/python -m pytest tests/ -x -q` exits 0 with 94+ tests passed
    - `requirements-dev.txt` contains the string `pytest-cov`
  </acceptance_criteria>
  <done>pytest-cov instalado, suite existente verde, baseline de cobertura confirmado >= 80%</done>
</task>

<task type="auto">
  <name>Task 2: Agregar tests de gaps del processor (TEST-01) y verificar queue (TEST-02)</name>
  <files>tests/test_processor.py</files>
  <read_first>
    - tests/test_processor.py
    - tests/conftest.py
    - app/processor.py
    - app/models.py
  </read_first>
  <action>
Agregar los siguientes 4 tests al final de tests/test_processor.py. Seguir el patron existente de clases de test (TestAutocrop, TestProcessImage). Per D-01, D-03, D-04, D-08: completar gaps sin reescribir tests existentes.

**Test 1: test_autocrop_bbox_none** (cubre processor.py linea 225)
Agregar a la clase TestAutocrop:
```python
def test_autocrop_bbox_none(self):
    """autocrop retorna imagen original cuando alpha es todo cero (bbox=None)."""
    # Imagen RGBA con todos los pixeles alpha=0 (completamente transparente)
    img = Image.new("RGBA", (200, 200), (0, 0, 0, 0))
    config = default_config()
    result = autocrop(img, config)
    assert result.size == (200, 200)
    assert result.mode == "RGBA"
```

**Test 2: test_autocrop_removes_small_artifacts** (cubre processor.py lineas 190-203)
Agregar a la clase TestAutocrop:
```python
def test_autocrop_removes_small_artifacts(self):
    """_clean_alpha_artifacts elimina artefacto pequeno; autocrop recorta solo el producto."""
    img = Image.new("RGBA", (200, 200), (0, 0, 0, 0))
    pixels = img.load()
    # Producto principal 50x50 en el centro (2500 pixeles opacos)
    for y in range(75, 125):
        for x in range(75, 125):
            pixels[x, y] = (255, 0, 0, 255)
    # Artefacto pequeno 2x2 en esquina (4 pixeles = 0.16% del producto)
    for y in range(5, 7):
        for x in range(5, 7):
            pixels[x, y] = (255, 0, 0, 255)
    config = default_config()
    cropped = autocrop(img, config)
    # Sin el artefacto, el bbox es solo el producto 50x50
    assert cropped.width == 50
    assert cropped.height == 50
```

**Test 3: test_pipeline_unknown_exception_wrapped** (cubre processor.py lineas 467-470)
Agregar a la clase TestProcessImage:
```python
def test_pipeline_unknown_exception_wrapped(self, sample_jpeg):
    """Excepcion no-ProcessingError en pipeline se envuelve en ProcessingError(step='unknown')."""
    config = default_config()
    mock_session = MagicMock()
    with patch("app.processor.autocrop", side_effect=ValueError("unexpected error")):
        with pytest.raises(ProcessingError) as exc_info:
            process_image(sample_jpeg, "ART-ERR", config, mock_session)
    assert exc_info.value.step == "unknown"
    assert "unexpected error" in exc_info.value.detail
```

**Test 4: test_process_image_enhance_not_in_steps_when_default** (cubre processor.py lineas 457-458, 461)
Agregar a la clase TestProcessImage:
```python
def test_process_image_enhance_not_in_steps_when_default(self, sample_jpeg):
    """Pipeline con brightness=1.0 y contrast=1.0 NO incluye 'enhance' en steps_applied."""
    config = default_config()
    # Asegurar valores default (no enhancement)
    config.enhancement.brightness = 1.0
    config.enhancement.contrast = 1.0
    mock_session = MagicMock()
    mock_rgba = Image.new("RGBA", (200, 150), (255, 0, 0, 255))
    mock_rgba_bytes = io.BytesIO()
    mock_rgba.save(mock_rgba_bytes, format="PNG")
    with patch("rembg.remove", return_value=mock_rgba):
        result = process_image(sample_jpeg, "ART-ENH", config, mock_session)
    assert "enhance" not in result.steps_applied
    assert "encode" in result.steps_applied
```

Notas:
- Importar `io` si no esta importado al inicio del archivo
- Importar `ProcessingError` de `app.models` si no esta importado
- Usar `from unittest.mock import patch, MagicMock` (ya deberia estar importado)
- `default_config()` ya existe como helper en test_processor.py
- Per D-03: estos tests cubren los edge cases documentados (alpha artifacts, bbox none, exception wrapping, enhancement skip)
- Per D-07 / D-08: no se modifica codigo fuente, solo se agregan tests

Luego correr los tests de queue para verificar TEST-02:
```bash
.venv/bin/python -m pytest tests/test_queue.py -x -q
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_processor.py tests/test_queue.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_autocrop_bbox_none" tests/test_processor.py` returns 1
    - `grep -c "test_autocrop_removes_small_artifacts" tests/test_processor.py` returns 1
    - `grep -c "test_pipeline_unknown_exception_wrapped" tests/test_processor.py` returns 1
    - `grep -c "test_process_image_enhance_not_in_steps_when_default" tests/test_processor.py` returns 1
    - `.venv/bin/python -m pytest tests/test_processor.py -x -q` exits 0
    - `.venv/bin/python -m pytest tests/test_queue.py -x -q` exits 0 (TEST-02 verified)
  </acceptance_criteria>
  <done>4 tests nuevos en test_processor.py pasando en verde. Tests de queue siguen en verde (TEST-02 confirmado con 100% cobertura)</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_processor.py tests/test_queue.py --cov=app/processor --cov=app/queue --cov-report=term-missing -q` muestra processor.py con las lineas 180, 190-203, 225, 457-458, 467-470 cubiertas
- Queue.py sigue en 100% cobertura
</verification>

<success_criteria>
- 4 tests nuevos en test_processor.py, todos pasando
- pytest-cov instalado y funcional
- Tests de queue (TEST-02) verificados en verde con 100% cobertura
- Cobertura total del proyecto >= 80%
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests-hardening/05-01-SUMMARY.md`
</output>
