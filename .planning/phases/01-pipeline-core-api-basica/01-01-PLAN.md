---
phase: 01-pipeline-core-api-basica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - requirements-dev.txt
  - config/settings.yaml
  - app/__init__.py
  - app/models.py
  - app/config.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements:
  - CONF-01
  - CONF-06
  - PIPE-10

must_haves:
  truths:
    - "La configuracion se carga desde config/settings.yaml con yaml.safe_load"
    - "get_snapshot() retorna una copia inmutable del config activo"
    - "Los modelos Pydantic validan la estructura de config, request y response"
  artifacts:
    - path: "pyproject.toml"
      provides: "Configuracion de proyecto y pytest"
      contains: "asyncio_mode"
    - path: "config/settings.yaml"
      provides: "Config default del servicio"
      contains: "birefnet-lite"
    - path: "app/models.py"
      provides: "Pydantic models para config, processing result, error response"
      exports: ["AppConfig", "ProcessingResult", "ErrorResponse"]
    - path: "app/config.py"
      provides: "ConfigManager con carga YAML y snapshot inmutable"
      exports: ["ConfigManager"]
    - path: "tests/conftest.py"
      provides: "Fixtures compartidos para toda la test suite"
      contains: "sample_jpeg"
  key_links:
    - from: "app/config.py"
      to: "app/models.py"
      via: "import AppConfig"
      pattern: "from app\\.models import.*AppConfig"
    - from: "app/config.py"
      to: "config/settings.yaml"
      via: "yaml.safe_load"
      pattern: "yaml\\.safe_load"
---

<objective>
Scaffold del proyecto completo, modelos Pydantic, ConfigManager con carga YAML y snapshot inmutable, y test infrastructure base.

Purpose: Establece la fundacion sobre la que se construyen processor, queue y API. Sin config y models, nada puede funcionar.
Output: Proyecto funcional con pyproject.toml, requirements, settings.yaml, models.py, config.py, y test scaffolds listos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-core-api-basica/01-CONTEXT.md
@PRD-image-standardizer-v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold del proyecto + dependencias + settings.yaml</name>
  <files>pyproject.toml, requirements.txt, requirements-dev.txt, config/settings.yaml, app/__init__.py, tests/__init__.py</files>
  <read_first>PRD-image-standardizer-v2.md, CLAUDE.md</read_first>
  <action>
Crear la estructura base del proyecto:

1. `pyproject.toml` con:
   ```toml
   [project]
   name = "image-standardizer"
   version = "1.0.0"
   requires-python = ">=3.11"

   [tool.pytest.ini_options]
   asyncio_mode = "auto"
   testpaths = ["tests"]
   ```

2. `requirements.txt` con versiones exactas:
   ```
   fastapi==0.135.2
   uvicorn[standard]==0.42.0
   rembg[cpu]==2.0.74
   Pillow==12.1.1
   typer==0.24.1
   Jinja2==3.1.6
   PyYAML==6.0.3
   watchdog==6.0.0
   python-multipart>=0.0.9
   ```

3. `requirements-dev.txt`:
   ```
   -r requirements.txt
   pytest==9.0.2
   pytest-asyncio==1.3.0
   httpx==0.28.1
   pytest-cov>=6.0
   ```

4. `config/settings.yaml` con contenido EXACTO del PRD seccion 5 (rembg.model: "birefnet-lite", output.size: 800, output.format: "webp", output.quality: 85, output.background_color: [255, 255, 255], padding.enabled: true, padding.percent: 10, autocrop.enabled: true, autocrop.threshold: 10, enhancement.brightness: 1.0, enhancement.contrast: 1.0, queue.max_concurrent: 1, queue.max_queue_size: 10, queue.timeout_seconds: 120, server.host: "0.0.0.0", server.port: 8010, server.log_level: "info")

5. `app/__init__.py` y `tests/__init__.py` vacios.
  </action>
  <verify>
    <automated>python -c "import yaml; c=yaml.safe_load(open('config/settings.yaml')); assert c['rembg']['model']=='birefnet-lite'; assert c['output']['size']==800; assert c['queue']['max_concurrent']==1; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contiene `asyncio_mode = "auto"`
    - requirements.txt contiene `fastapi==0.135.2` y `rembg[cpu]==2.0.74`
    - config/settings.yaml contiene `model: "birefnet-lite"` y `size: 800` y `quality: 85`
    - app/__init__.py existe (puede estar vacio)
    - tests/__init__.py existe (puede estar vacio)
  </acceptance_criteria>
  <done>Proyecto scaffoldeado con todas las dependencias pinned y settings.yaml identico al PRD</done>
</task>

<task type="auto">
  <name>Task 2: Pydantic models + ConfigManager + tests</name>
  <files>app/models.py, app/config.py, tests/conftest.py, tests/test_config.py</files>
  <read_first>config/settings.yaml, PRD-image-standardizer-v2.md, CLAUDE.md</read_first>
  <action>
1. `app/models.py` — Pydantic v2 models que mapean 1:1 con settings.yaml:

   ```python
   from pydantic import BaseModel
   from typing import Optional
   from datetime import datetime

   class RembgConfig(BaseModel):
       model: str = "birefnet-lite"
       alpha_matting: bool = False
       alpha_matting_foreground_threshold: int = 240
       alpha_matting_background_threshold: int = 10
       alpha_matting_erode_size: int = 10

   class OutputConfig(BaseModel):
       size: int = 800
       format: str = "webp"
       quality: int = 85
       background_color: list[int] = [255, 255, 255]

   class PaddingConfig(BaseModel):
       enabled: bool = True
       percent: int = 10

   class AutocropConfig(BaseModel):
       enabled: bool = True
       threshold: int = 10

   class EnhancementConfig(BaseModel):
       brightness: float = 1.0
       contrast: float = 1.0

   class QueueConfig(BaseModel):
       max_concurrent: int = 1
       max_queue_size: int = 10
       timeout_seconds: int = 120

   class ServerConfig(BaseModel):
       host: str = "0.0.0.0"
       port: int = 8010
       log_level: str = "info"

   class AppConfig(BaseModel):
       rembg: RembgConfig = RembgConfig()
       output: OutputConfig = OutputConfig()
       padding: PaddingConfig = PaddingConfig()
       autocrop: AutocropConfig = AutocropConfig()
       enhancement: EnhancementConfig = EnhancementConfig()
       queue: QueueConfig = QueueConfig()
       server: ServerConfig = ServerConfig()

   class ProcessingResult(BaseModel):
       image_bytes: bytes
       article_id: str
       processing_time_ms: int
       model_used: str
       original_size: str       # "WxH"
       output_size: str         # "WxH"
       steps_applied: list[str]

   class ErrorResponse(BaseModel):
       error: str
       detail: str
       article_id: Optional[str] = None
   ```

2. `app/config.py` — ConfigManager:

   ```python
   import yaml
   from pathlib import Path
   from app.models import AppConfig
   import copy

   class ConfigManager:
       def __init__(self, config_path: str = "config/settings.yaml"):
           self._config_path = Path(config_path)
           self._config: AppConfig = self._load()

       def _load(self) -> AppConfig:
           if self._config_path.exists():
               with open(self._config_path) as f:
                   data = yaml.safe_load(f) or {}
               return AppConfig(**data)
           return AppConfig()

       def reload(self) -> None:
           self._config = self._load()

       @property
       def config(self) -> AppConfig:
           return self._config

       def get_snapshot(self) -> AppConfig:
           """Retorna copia profunda inmutable para un job. Per CONF-06."""
           return self._config.model_copy(deep=True)
   ```

3. `tests/conftest.py` con fixtures compartidos:
   - `sample_jpeg` — genera imagen JPEG 200x150 RGB en bytes usando Pillow: `Image.new("RGB", (200, 150), (255, 0, 0))` guardada en BytesIO con format="JPEG"
   - `sample_png_transparent` — imagen PNG 100x100 RGBA con ~50% pixeles transparentes (mitad superior opaca, mitad inferior alpha=0)
   - `sample_cmyk` — imagen CMYK 100x100 guardada como TIFF en bytes
   - `sample_large_image` — imagen 6000x5000 RGB JPEG (>25 megapixels) para test de D-05
   - `tmp_settings_yaml` — crea un archivo temporal settings.yaml con el contenido default, retorna su path, limpia despues
   - `config_manager` — fixture que usa tmp_settings_yaml para crear un ConfigManager

4. `tests/test_config.py`:
   - `test_config_loads_yaml` — verifica que ConfigManager carga settings.yaml y retorna AppConfig con model=="birefnet-lite", output.size==800, queue.max_concurrent==1
   - `test_config_defaults_without_file` — ConfigManager con path inexistente retorna AppConfig con defaults
   - `test_config_snapshot_immutable` — get_snapshot() retorna una copia; modificar la copia no afecta el original (per CONF-06): `snap = cm.get_snapshot(); snap.output.size = 1200; assert cm.config.output.size == 800`
   - `test_config_reload` — modificar el YAML en disco y llamar reload() actualiza la config
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && pip install -e ".[dev]" 2>/dev/null || pip install -r requirements-dev.txt && pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - app/models.py contiene `class AppConfig(BaseModel)` con campos rembg, output, padding, autocrop, enhancement, queue, server
    - app/models.py contiene `class ProcessingResult(BaseModel)` con campo `steps_applied: list[str]`
    - app/models.py contiene `class ErrorResponse(BaseModel)` con campos error, detail, article_id (per D-01)
    - app/config.py contiene `class ConfigManager` con metodos `_load`, `reload`, `get_snapshot`
    - app/config.py usa `yaml.safe_load` (nunca yaml.load sin Loader)
    - tests/conftest.py contiene fixtures `sample_jpeg`, `sample_png_transparent`, `sample_cmyk`, `sample_large_image`, `config_manager`
    - `pytest tests/test_config.py -x` sale con exit code 0
  </acceptance_criteria>
  <done>Models Pydantic mapean 1:1 con settings.yaml, ConfigManager carga YAML y get_snapshot() retorna copia profunda inmutable, 4 tests pasan en verde</done>
</task>

</tasks>

<verification>
- `pytest tests/test_config.py -x -v` pasa los 4 tests
- `python -c "from app.models import AppConfig, ProcessingResult, ErrorResponse; print('imports OK')"` exitoso
- `python -c "from app.config import ConfigManager; cm = ConfigManager(); print(cm.config.rembg.model)"` imprime "birefnet-lite"
</verification>

<success_criteria>
- Proyecto scaffoldeado con pyproject.toml, requirements.txt, requirements-dev.txt
- settings.yaml identico al PRD seccion 5
- AppConfig, ProcessingResult, ErrorResponse definidos en models.py
- ConfigManager carga YAML y soporta snapshot inmutable
- Test suite base funcional (conftest.py con fixtures, test_config.py verde)
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-core-api-basica/01-01-SUMMARY.md`
</output>
