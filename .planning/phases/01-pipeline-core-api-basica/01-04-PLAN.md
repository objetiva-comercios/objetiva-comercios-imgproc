---
phase: 01-pipeline-core-api-basica
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - app/main.py
  - app/router_api.py
  - tests/test_api.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - PIPE-10

must_haves:
  truths:
    - "POST /process acepta multipart/form-data con image file y article_id string"
    - "POST /process retorna image/webp con los 6 headers X-* de metadata"
    - "POST /process acepta override parcial de config como JSON string"
    - "POST /process retorna 400, 422, 503, 504 segun el caso, con JSON estructurado"
    - "GET /health retorna status, queue state, model info y uptime"
    - "La sesion rembg se inicializa una sola vez en lifespan"
  artifacts:
    - path: "app/main.py"
      provides: "FastAPI app con lifespan, rembg session global, routers incluidos"
      exports: ["app"]
      min_lines: 40
    - path: "app/router_api.py"
      provides: "Endpoints POST /process y GET /health"
      exports: ["router"]
      min_lines: 80
    - path: "tests/test_api.py"
      provides: "Tests de integracion de endpoints HTTP"
      min_lines: 100
  key_links:
    - from: "app/main.py"
      to: "app/config.py"
      via: "ConfigManager initialization in lifespan"
      pattern: "ConfigManager"
    - from: "app/main.py"
      to: "app/queue.py"
      via: "JobQueue initialization in lifespan"
      pattern: "JobQueue"
    - from: "app/main.py"
      to: "rembg"
      via: "new_session() in lifespan startup"
      pattern: "new_session"
    - from: "app/router_api.py"
      to: "app/queue.py"
      via: "submit_job() en endpoint /process"
      pattern: "submit_job"
    - from: "app/router_api.py"
      to: "app/processor.py"
      via: "process_image pasado como process_fn a submit_job"
      pattern: "process_image"
---

<objective>
Implementar la FastAPI app con lifespan (rembg session + queue + config), los endpoints POST /process y GET /health, y los tests de integracion de la API.

Purpose: Conecta el processor y el queue al mundo exterior via HTTP. Es lo que n8n va a llamar.
Output: main.py con la app FastAPI, router_api.py con endpoints, test_api.py con tests de integracion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-core-api-basica/01-CONTEXT.md
@PRD-image-standardizer-v2.md

<interfaces>
From app/models.py:
```python
class AppConfig(BaseModel): ...
class ProcessingResult(BaseModel):
    image_bytes: bytes
    article_id: str
    processing_time_ms: int
    model_used: str
    original_size: str
    output_size: str
    steps_applied: list[str]
class ErrorResponse(BaseModel):
    error: str
    detail: str
    article_id: str | None = None
```

From app/config.py:
```python
class ConfigManager:
    def __init__(self, config_path: str = "config/settings.yaml"): ...
    def get_snapshot(self) -> AppConfig: ...
    @property
    def config(self) -> AppConfig: ...
```

From app/processor.py:
```python
class ProcessingError(Exception):
    def __init__(self, step: str, detail: str): ...
def process_image(image_bytes: bytes, article_id: str, config: AppConfig, rembg_session) -> ProcessingResult: ...
```

From app/queue.py:
```python
class JobQueue:
    def __init__(self, max_concurrent, max_queue_size, timeout_seconds): ...
    async def submit_job(self, process_fn, image_bytes, article_id, config_snapshot, rembg_session): ...
    @property
    def state(self) -> QueueState: ...
    @property
    def max_concurrent(self) -> int: ...
class QueueFullError(Exception): ...
class QueueTimeoutError(Exception): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI app + lifespan + router_api.py</name>
  <files>app/main.py, app/router_api.py</files>
  <read_first>app/models.py, app/config.py, app/processor.py, app/queue.py, PRD-image-standardizer-v2.md, CLAUDE.md</read_first>
  <action>
1. Crear `app/main.py`:

```python
import asyncio
import time
import json
import logging
from contextlib import asynccontextmanager
from fastapi import FastAPI
from rembg import new_session
from app.config import ConfigManager
from app.queue import JobQueue

logger = logging.getLogger(__name__)

@asynccontextmanager
async def lifespan(app: FastAPI):
    """Inicializa recursos globales al startup, limpia al shutdown."""
    start = time.monotonic()

    # 1. Cargar config
    config_manager = ConfigManager()
    app.state.config_manager = config_manager
    config = config_manager.config

    logger.info(json.dumps({
        "level": "info",
        "event": "config_loaded",
        "model": config.rembg.model,
    }))

    # 2. Inicializar sesion rembg GLOBAL (una sola vez, NUNCA por request)
    # OMP_NUM_THREADS debe estar seteado en el environment (Dockerfile)
    rembg_session = new_session(config.rembg.model)
    app.state.rembg_session = rembg_session
    app.state.model_loaded = True
    app.state.model_name = config.rembg.model

    logger.info(json.dumps({
        "level": "info",
        "event": "rembg_session_ready",
        "model": config.rembg.model,
    }))

    # 3. Inicializar job queue
    queue = JobQueue(
        max_concurrent=config.queue.max_concurrent,
        max_queue_size=config.queue.max_queue_size,
        timeout_seconds=config.queue.timeout_seconds,
    )
    app.state.job_queue = queue

    # 4. Guardar startup time para /health uptime
    app.state.startup_time = time.monotonic()

    logger.info(json.dumps({
        "level": "info",
        "event": "startup_complete",
        "elapsed_ms": int((time.monotonic() - start) * 1000),
    }))

    yield

    # Shutdown
    logger.info(json.dumps({"level": "info", "event": "shutdown"}))

app = FastAPI(
    title="Image Standardizer",
    version="1.0.0",
    lifespan=lifespan,
)

# Importar y registrar routers
from app.router_api import router as api_router
app.include_router(api_router)
```

2. Crear `app/router_api.py`:

```python
import json
import logging
from typing import Optional
from fastapi import APIRouter, UploadFile, File, Form, Request, Response
from fastapi.responses import JSONResponse
from app.processor import process_image, ProcessingError
from app.queue import QueueFullError, QueueTimeoutError
from app.models import ErrorResponse

logger = logging.getLogger(__name__)
router = APIRouter()

@router.post("/process")
async def process_endpoint(
    request: Request,
    image: UploadFile = File(...),
    article_id: str = Form(...),
    override: Optional[str] = Form(default=None),
):
    """
    Procesa una imagen de producto y retorna WebP estandarizado.
    Per API-01, API-02, API-03, API-04.
    """
    queue = request.app.state.job_queue
    config_manager = request.app.state.config_manager
    rembg_session = request.app.state.rembg_session

    # Tomar snapshot del config (per CONF-06)
    config_snapshot = config_manager.get_snapshot()

    # Aplicar override parcial si viene (per API-03)
    if override:
        try:
            import json as json_mod
            override_dict = json_mod.loads(override)
            # Deep merge: convertir config a dict, mergear, recrear
            config_dict = config_snapshot.model_dump()
            _deep_merge(config_dict, override_dict)
            from app.models import AppConfig
            config_snapshot = AppConfig(**config_dict)
        except (json_mod.JSONDecodeError, Exception) as e:
            return JSONResponse(
                status_code=422,
                content=ErrorResponse(
                    error="invalid_override",
                    detail=f"Invalid override JSON: {str(e)}",
                    article_id=article_id,
                ).model_dump(),
            )

    # Leer bytes de la imagen
    image_bytes = await image.read()

    try:
        result = await queue.submit_job(
            process_fn=process_image,
            image_bytes=image_bytes,
            article_id=article_id,
            config_snapshot=config_snapshot,
            rembg_session=rembg_session,
        )

        # Retornar WebP con headers de metadata (per API-02)
        return Response(
            content=result.image_bytes,
            media_type="image/webp",
            headers={
                "X-Article-Id": result.article_id,
                "X-Processing-Time-Ms": str(result.processing_time_ms),
                "X-Model-Used": result.model_used,
                "X-Original-Size": result.original_size,
                "X-Output-Size": result.output_size,
                "X-Steps-Applied": ",".join(result.steps_applied),
            },
        )

    except QueueFullError:
        return JSONResponse(
            status_code=503,
            content=ErrorResponse(
                error="queue_full",
                detail="Service busy, try again later",
                article_id=article_id,
            ).model_dump(),
        )

    except QueueTimeoutError:
        return JSONResponse(
            status_code=504,
            content=ErrorResponse(
                error="queue_timeout",
                detail="Request timed out waiting in queue",
                article_id=article_id,
            ).model_dump(),
        )

    except ProcessingError as e:
        # Per D-09: fail fast con detalle del step
        status = 400 if e.step == "decode" else 500
        return JSONResponse(
            status_code=status,
            content=ErrorResponse(
                error=f"processing_error_{e.step}",
                detail=e.detail if e.step == "decode" else f"Processing failed at step: {e.step}",
                article_id=article_id,
            ).model_dump(),
        )

    except Exception as e:
        logger.error(json.dumps({
            "level": "error",
            "event": "unhandled_error",
            "article_id": article_id,
            "error": str(e),
        }))
        return JSONResponse(
            status_code=500,
            content=ErrorResponse(
                error="internal_error",
                detail="Internal server error",
                article_id=article_id,
            ).model_dump(),
        )


@router.get("/health")
async def health_endpoint(request: Request):
    """
    Health check rapido (per API-05).
    Lee app.state directamente, sin tocar el queue.
    """
    import time
    queue = request.app.state.job_queue
    uptime = int(time.monotonic() - request.app.state.startup_time)

    return {
        "status": "ok",
        "queue": {
            "active_jobs": queue.state.active_jobs,
            "queued_jobs": queue.state.queued_jobs,
            "max_concurrent": queue.max_concurrent,
            "max_queue_size": queue._max_queue_size,
        },
        "model_loaded": request.app.state.model_loaded,
        "model_name": request.app.state.model_name,
        "uptime_seconds": uptime,
    }


def _deep_merge(base: dict, override: dict) -> None:
    """Deep merge override into base dict (in-place)."""
    for key, value in override.items():
        if key in base and isinstance(base[key], dict) and isinstance(value, dict):
            _deep_merge(base[key], value)
        else:
            base[key] = value
```

IMPORTANTE:
- El error 400 para imagen corrupta usa "Invalid or corrupt image" per D-02 (el ProcessingError del decode step ya tiene ese mensaje)
- Los errores 500 de pipeline NO exponen internals — solo dicen "Processing failed at step: {step}"
- Los JSON de error siguen el formato de D-01: `{"error": "tipo", "detail": "mensaje", "article_id": "..."}`
- El logging es JSON structured per D-03
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -c "from app.main import app; print('app created:', app.title)"</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py contiene `@asynccontextmanager` y `async def lifespan`
    - app/main.py contiene `new_session(config.rembg.model)` en lifespan
    - app/main.py contiene `app.state.rembg_session =`
    - app/main.py contiene `app.state.startup_time =`
    - app/main.py contiene `app = FastAPI(` con lifespan=lifespan
    - app/router_api.py contiene `@router.post("/process")` con parametros image, article_id, override
    - app/router_api.py contiene `@router.get("/health")`
    - app/router_api.py retorna `Response(content=result.image_bytes, media_type="image/webp"` con 6 headers X-*
    - app/router_api.py maneja QueueFullError con 503, QueueTimeoutError con 504, ProcessingError con 400/500
    - app/router_api.py contiene `def _deep_merge` para override parcial
  </acceptance_criteria>
  <done>FastAPI app con lifespan completo, POST /process con todos los error codes, GET /health con queue state</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests de integracion de la API</name>
  <files>tests/test_api.py</files>
  <read_first>app/main.py, app/router_api.py, app/processor.py, app/queue.py, app/models.py, tests/conftest.py</read_first>
  <behavior>
    - test_process_success: POST /process con JPEG valido + article_id -> 200, content-type image/webp, body es WebP valido
    - test_process_headers: POST /process exitoso -> response tiene headers X-Article-Id, X-Processing-Time-Ms, X-Model-Used, X-Original-Size, X-Output-Size, X-Steps-Applied
    - test_process_override: POST /process con override={"output":{"size":400}} -> X-Output-Size contiene "400x400"
    - test_process_400_corrupt: POST /process con bytes random -> 400 con error "processing_error_decode"
    - test_process_422_missing_article_id: POST /process sin article_id -> 422
    - test_process_503_queue_full: mockear queue llena -> 503 con error "queue_full"
    - test_health: GET /health -> 200 con status "ok", model_loaded true, uptime_seconds >= 0
    - test_health_has_queue_state: GET /health -> response tiene queue.active_jobs, queue.queued_jobs, queue.max_concurrent
  </behavior>
  <action>
Crear `tests/test_api.py`:

- Agregar fixture `async_client` en conftest.py (si no existe) o en test_api.py:
  ```python
  import pytest
  from httpx import AsyncClient, ASGITransport
  from unittest.mock import patch, MagicMock, AsyncMock
  from app.main import app
  from app.models import ProcessingResult

  @pytest.fixture
  async def async_client():
      # Mockear rembg.new_session para no cargar modelo real en tests
      mock_session = MagicMock()
      with patch("app.main.new_session", return_value=mock_session):
          async with AsyncClient(
              transport=ASGITransport(app=app),
              base_url="http://test",
          ) as client:
              yield client
  ```

- Para test_process_success y tests que necesitan pipeline completo: mockear `app.processor.process_image` para retornar un ProcessingResult fake con image_bytes de un WebP real pequeno (generar uno con Pillow en el fixture). Esto evita cargar rembg en tests.

  ```python
  def _make_fake_result(article_id="TEST-001"):
      from io import BytesIO
      from PIL import Image
      buf = BytesIO()
      Image.new("RGB", (800, 800), (255, 255, 255)).save(buf, format="WEBP", quality=85)
      return ProcessingResult(
          image_bytes=buf.getvalue(),
          article_id=article_id,
          processing_time_ms=100,
          model_used="birefnet-lite",
          original_size="200x150",
          output_size="800x800",
          steps_applied=["decode", "rembg", "autocrop", "scale", "composite", "encode"],
      )
  ```

- Para test_process_400_corrupt: enviar bytes random (b"not an image") como image file
- Para test_process_422: enviar imagen sin article_id form field
- Para test_process_503: mockear queue.submit_job para levantar QueueFullError
- Para test_health: GET /health sin mocks adicionales (lifespan ya inicializo todo con mocks)

IMPORTANTE: Todos los tests deben mockear rembg (tanto new_session en lifespan como process_image para evitar carga del modelo ONNX). Los tests verifican la API HTTP, no el procesamiento real.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && pytest tests/test_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_api.py contiene fixture `async_client` que mockea rembg.new_session
    - tests/test_api.py contiene `test_process_success` que verifica status 200 y content-type image/webp
    - tests/test_api.py contiene `test_process_headers` que verifica los 6 headers X-*
    - tests/test_api.py contiene `test_process_override` que verifica override funciona
    - tests/test_api.py contiene `test_process_400_corrupt` que verifica status 400
    - tests/test_api.py contiene `test_process_422_missing_article_id` o similar que verifica 422
    - tests/test_api.py contiene `test_process_503_queue_full` que verifica status 503
    - tests/test_api.py contiene `test_health` que verifica status 200 y estructura del response
    - `pytest tests/test_api.py -x` sale con exit code 0
  </acceptance_criteria>
  <done>8+ tests de integracion pasan verificando todos los endpoints y error codes de la API</done>
</task>

</tasks>

<verification>
- `pytest tests/test_api.py -x -v` pasa los 8+ tests
- `pytest tests/ -x -v` (toda la suite) pasa
- `python -c "from app.main import app; print(app.routes)"` muestra /process y /health
</verification>

<success_criteria>
- FastAPI app con lifespan inicializa rembg session global, ConfigManager y JobQueue
- POST /process acepta multipart, retorna WebP con 6 headers, maneja 400/422/503/504
- GET /health retorna estado completo sin tocar el queue
- Override parcial via JSON string funciona con deep merge
- 8+ tests de integracion pasan con rembg mockeado
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-core-api-basica/01-04-SUMMARY.md`
</output>
