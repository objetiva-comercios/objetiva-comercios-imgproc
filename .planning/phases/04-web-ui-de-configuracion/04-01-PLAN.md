---
phase: 04-web-ui-de-configuracion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/router_ui.py
  - app/main.py
  - app/router_api.py
  - app/templates/ui.html
  - tests/test_ui.py
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "GET /ui retorna 200 con Content-Type text/html"
    - "GET /health incluye campo model_swapping en la respuesta JSON"
    - "El template Jinja2 se resuelve con path absoluto via Path(__file__).parent"
  artifacts:
    - path: "app/router_ui.py"
      provides: "Endpoint GET /ui con TemplateResponse"
      exports: ["router"]
    - path: "app/templates/ui.html"
      provides: "Template HTML stub para Jinja2"
      min_lines: 10
    - path: "tests/test_ui.py"
      provides: "Tests de integracion del endpoint /ui"
      min_lines: 50
  key_links:
    - from: "app/main.py"
      to: "app/router_ui.py"
      via: "app.include_router(ui_router)"
      pattern: "include_router.*ui_router"
    - from: "app/router_ui.py"
      to: "app/templates/ui.html"
      via: "Jinja2Templates con Path(__file__).parent / templates"
      pattern: "Path.*__file__.*templates"
---

<objective>
Crear el backend para la Web UI: endpoint GET /ui con Jinja2 TemplateResponse, agregar model_swapping al health endpoint, crear scaffold de tests, y un template HTML stub funcional que pase los tests basicos.

Purpose: Establecer la infraestructura backend y los tests antes de construir la UI completa en Plan 02. El template stub sirve como contrato verificable — Plan 02 lo reemplaza con la UI final.
Output: router_ui.py registrado en main.py, /health con model_swapping, test_ui.py con 8+ tests, template stub funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-ui-de-configuracion/04-CONTEXT.md
@.planning/phases/04-web-ui-de-configuracion/04-RESEARCH.md

<interfaces>
<!-- Contratos existentes que el executor necesita -->

From app/models.py:
```python
class AppConfig(BaseModel):
    rembg: RembgConfig = RembgConfig()
    output: OutputConfig = OutputConfig()
    padding: PaddingConfig = PaddingConfig()
    autocrop: AutocropConfig = AutocropConfig()
    enhancement: EnhancementConfig = EnhancementConfig()
    queue: QueueConfig = QueueConfig()
    server: ServerConfig = ServerConfig()
```

From app/router_config.py:
```python
VALID_MODELS = frozenset(sessions_names)  # whitelist de modelos rembg

@router.get("/config")
async def get_config(request: Request): ...

@router.post("/config")
async def update_config(request: Request): ...

@router.get("/status")
async def status_endpoint(request: Request): ...
```

From app/router_api.py:
```python
@router.get("/health")
async def health_endpoint(request: Request):
    # Retorna dict con: status, queue, model_loaded, model_name, uptime_seconds
    # FALTA: model_swapping (Pitfall 1 de RESEARCH.md)
```

From app/main.py (lineas 228-232):
```python
from app.router_api import router as api_router
app.include_router(api_router)

from app.router_config import router as config_router
app.include_router(config_router)
```

Test pattern from tests/test_api.py:
```python
@pytest.fixture
async def client():
    with patch("rembg.new_session", return_value=MagicMock()):
        async with app.router.lifespan_context(app):
            async with AsyncClient(
                transport=ASGITransport(app=app),
                base_url="http://test",
            ) as client:
                yield client
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router UI + health fix + main.py registration</name>
  <files>app/router_ui.py, app/main.py, app/router_api.py, app/templates/ui.html</files>
  <read_first>
    - app/main.py (ver patron de include_router existente, lineas 228-232)
    - app/router_api.py (ver health_endpoint lineas 167-189 — agregar model_swapping)
    - app/router_config.py (ver VALID_MODELS import y get_config para contexto de template)
    - app/models.py (ver AppConfig completo para contexto de template)
  </read_first>
  <action>
1. Crear directorio `app/templates/` si no existe.

2. Crear `app/router_ui.py` con:
   - `from pathlib import Path`
   - `from fastapi import APIRouter, Request`
   - `from fastapi.responses import HTMLResponse`
   - `from fastapi.templating import Jinja2Templates`
   - `router = APIRouter()`
   - `_TEMPLATES_DIR = Path(__file__).parent / "templates"` (path absoluto — Pitfall 2 de RESEARCH.md)
   - `templates = Jinja2Templates(directory=str(_TEMPLATES_DIR))`
   - Endpoint `@router.get("/ui", response_class=HTMLResponse)` async que:
     a. Lee `config = request.app.state.config_manager.config`
     b. Importa `from app.router_config import VALID_MODELS`
     c. Importa `from app.models import AppConfig`
     d. Retorna `templates.TemplateResponse(request=request, name="ui.html", context={"config": config.model_dump(), "config_defaults": AppConfig().model_dump(), "valid_models": sorted(VALID_MODELS)})`

3. Crear `app/templates/ui.html` — template stub funcional (sera reemplazado por Plan 02):
   ```html
   <!DOCTYPE html>
   <html lang="es">
   <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Image Standardizer - Config</title>
     <style>
       :root { --bg-primary: #ffffff; --text-primary: #1a1a1a; }
       @media (prefers-color-scheme: dark) {
         :root { --bg-primary: #0f0f0f; --text-primary: #f0f0f0; }
       }
       body { font-family: 'Inter', system-ui, sans-serif; background: var(--bg-primary); color: var(--text-primary); }
     </style>
   </head>
   <body>
     <h1>Image Standardizer</h1>
     <div id="status-card"></div>
     <div id="swap-banner" style="display:none">Model swap en progreso...</div>
     <form id="config-form">
       <select id="rembg-model">
         {% for model in valid_models %}
         <option value="{{ model }}" {% if model == config.rembg.model %}selected{% endif %}>{{ model }}</option>
         {% endfor %}
       </select>
       <input type="checkbox" id="rembg-alpha-matting" {% if config.rembg.alpha_matting %}checked{% endif %}>
       <input type="number" id="am-fg" value="{{ config.rembg.alpha_matting_foreground_threshold }}">
       <input type="number" id="am-bg" value="{{ config.rembg.alpha_matting_background_threshold }}">
       <input type="number" id="am-erode" value="{{ config.rembg.alpha_matting_erode_size }}">
       <input type="number" id="output-size" value="{{ config.output.size }}">
       <input type="range" id="output-quality" min="1" max="100" value="{{ config.output.quality }}">
       <input type="color" id="output-bg-color">
       <input type="checkbox" id="padding-enabled" {% if config.padding.enabled %}checked{% endif %}>
       <input type="range" id="padding-percent" min="0" max="50" value="{{ config.padding.percent }}">
       <input type="checkbox" id="autocrop-enabled" {% if config.autocrop.enabled %}checked{% endif %}>
       <input type="number" id="autocrop-threshold" value="{{ config.autocrop.threshold }}">
       <input type="range" id="enhancement-brightness" min="0.1" max="3.0" step="0.1" value="{{ config.enhancement.brightness }}">
       <input type="range" id="enhancement-contrast" min="0.1" max="3.0" step="0.1" value="{{ config.enhancement.contrast }}">
       <input type="number" id="queue-max-concurrent" value="{{ config.queue.max_concurrent }}">
       <input type="number" id="queue-max-queue-size" value="{{ config.queue.max_queue_size }}">
       <input type="number" id="queue-timeout" value="{{ config.queue.timeout_seconds }}">
       <button type="button" id="save-btn" onclick="saveConfig()">Guardar</button>
       <button type="button" id="restore-btn" onclick="restoreDefaults()">Restaurar defaults</button>
       <button type="button" id="yaml-btn" onclick="toggleYaml()">Ver YAML</button>
     </form>
     <pre id="yaml-view" style="display:none"></pre>
     <div id="jobs-table"></div>
     <div id="toast" class="toast"></div>
     <script>
       const CONFIG_DEFAULTS = {{ config_defaults | tojson }};
       function rgbArrayToHex(arr) { return '#' + arr.map(v => v.toString(16).padStart(2,'0')).join(''); }
       function hexToRgbArray(hex) { const n = parseInt(hex.slice(1),16); return [(n>>16)&255,(n>>8)&255,n&255]; }
       document.getElementById('output-bg-color').value = rgbArrayToHex({{ config.output.background_color | tojson }});

       async function fetchHealth() {
         try {
           const r = await fetch('/health');
           const d = await r.json();
           document.getElementById('status-card').textContent = JSON.stringify(d);
           document.getElementById('swap-banner').style.display = d.model_swapping ? 'block' : 'none';
         } catch(e) {}
       }
       async function fetchStatus() {
         try {
           const r = await fetch('/status');
           const d = await r.json();
           document.getElementById('jobs-table').textContent = JSON.stringify(d.job_history?.slice(0,10));
         } catch(e) {}
       }
       function showToast(msg, type) {
         const t = document.getElementById('toast');
         t.textContent = msg; t.className = 'toast toast-' + type + ' visible';
         clearTimeout(t._timer);
         t._timer = setTimeout(() => { t.className = 'toast'; }, 3000);
       }
       async function saveConfig() {
         const payload = {
           rembg: { model: document.getElementById('rembg-model').value, alpha_matting: document.getElementById('rembg-alpha-matting').checked, alpha_matting_foreground_threshold: parseInt(document.getElementById('am-fg').value), alpha_matting_background_threshold: parseInt(document.getElementById('am-bg').value), alpha_matting_erode_size: parseInt(document.getElementById('am-erode').value) },
           output: { size: parseInt(document.getElementById('output-size').value), quality: parseInt(document.getElementById('output-quality').value), background_color: hexToRgbArray(document.getElementById('output-bg-color').value) },
           padding: { enabled: document.getElementById('padding-enabled').checked, percent: parseInt(document.getElementById('padding-percent').value) },
           autocrop: { enabled: document.getElementById('autocrop-enabled').checked, threshold: parseInt(document.getElementById('autocrop-threshold').value) },
           enhancement: { brightness: parseFloat(document.getElementById('enhancement-brightness').value), contrast: parseFloat(document.getElementById('enhancement-contrast').value) },
           queue: { max_concurrent: parseInt(document.getElementById('queue-max-concurrent').value), max_queue_size: parseInt(document.getElementById('queue-max-queue-size').value), timeout_seconds: parseInt(document.getElementById('queue-timeout').value) }
         };
         try {
           const r = await fetch('/config', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload) });
           if (r.ok) showToast('Configuracion guardada','success'); else { const e = await r.json(); showToast('Error: '+e.detail,'error'); }
         } catch(e) { showToast('Error de red','error'); }
       }
       async function restoreDefaults() {
         if (!confirm('Restaurar valores por defecto?')) return;
         try {
           const r = await fetch('/config', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(CONFIG_DEFAULTS) });
           if (r.ok) { showToast('Defaults restaurados','success'); location.reload(); }
         } catch(e) { showToast('Error','error'); }
       }
       async function toggleYaml() {
         const el = document.getElementById('yaml-view');
         if (el.style.display === 'none') {
           const r = await fetch('/config'); const d = await r.json();
           el.textContent = JSON.stringify(d, null, 2); el.style.display = 'block';
         } else { el.style.display = 'none'; }
       }
       fetchHealth(); fetchStatus();
       setInterval(fetchHealth, 5000); setInterval(fetchStatus, 5000);
     </script>
   </body>
   </html>
   ```
   Este template stub incluye TODOS los elementos funcionales que los tests verifican. Plan 02 lo reemplazara con la UI final usando la skill frontend-design.

4. Modificar `app/router_api.py` linea 178-189 — agregar `"model_swapping"` al dict de respuesta de `health_endpoint()`:
   Despues de la linea `"model_name": request.app.state.model_name,` agregar:
   `"model_swapping": getattr(request.app.state, "model_swapping", False),`
   Esto resuelve Pitfall 1 de RESEARCH.md y habilita D-04 (banner de swap en la UI).

5. Modificar `app/main.py` — agregar al final (despues de linea 232):
   ```python
   from app.router_ui import router as ui_router  # noqa: E402
   app.include_router(ui_router)
   ```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -c "from app.router_ui import router; print('router_ui OK')" && python -c "from app.main import app; routes = [r.path for r in app.routes]; assert '/ui' in routes, f'/ui not in {routes}'; print('/ui registered OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/router_ui.py contiene `Path(__file__).parent / "templates"`
    - app/router_ui.py contiene `@router.get("/ui", response_class=HTMLResponse)`
    - app/router_ui.py contiene `config_defaults`  y `valid_models` en el contexto del TemplateResponse
    - app/main.py contiene `from app.router_ui import router as ui_router`
    - app/main.py contiene `app.include_router(ui_router)`
    - app/router_api.py contiene `"model_swapping"` en health_endpoint
    - app/templates/ui.html contiene `prefers-color-scheme: dark`
    - app/templates/ui.html contiene `viewport`
    - app/templates/ui.html contiene `rembg-model`
    - app/templates/ui.html contiene `output-quality`
    - app/templates/ui.html contiene `setInterval`
    - app/templates/ui.html contiene `save-btn`
    - app/templates/ui.html contiene `restore-btn`
    - app/templates/ui.html contiene `/health`
  </acceptance_criteria>
  <done>GET /ui retorna 200 con HTML que contiene todos los controles de AppConfig, /health incluye model_swapping, router registrado en main.py</done>
</task>

<task type="auto">
  <name>Task 2: Tests de integracion para GET /ui</name>
  <files>tests/test_ui.py</files>
  <read_first>
    - tests/test_api.py (patron de fixture con lifespan mock — lineas 1-40)
    - tests/conftest.py (fixtures reutilizables: tmp_settings_yaml, config_manager)
    - app/router_ui.py (el archivo recien creado en Task 1)
    - app/templates/ui.html (el template stub recien creado en Task 1)
  </read_first>
  <action>
Crear `tests/test_ui.py` con los siguientes tests. Usar el patron de fixture de test_api.py (patch rembg.new_session + lifespan_context + AsyncClient con ASGITransport):

```python
import pytest
from httpx import AsyncClient, ASGITransport
from unittest.mock import patch, MagicMock

from app.main import app


@pytest.fixture
async def ui_client():
    with patch("rembg.new_session", return_value=MagicMock()):
        async with app.router.lifespan_context(app):
            async with AsyncClient(
                transport=ASGITransport(app=app),
                base_url="http://test",
            ) as client:
                yield client


async def test_ui_returns_html(ui_client):
    """UI-01: GET /ui retorna 200 con Content-Type text/html."""
    resp = await ui_client.get("/ui")
    assert resp.status_code == 200
    assert "text/html" in resp.headers["content-type"]
    assert "<html" in resp.text.lower()


async def test_ui_no_static_references(ui_client):
    """UI-01: HTML autocontenido — sin referencias a /static/."""
    resp = await ui_client.get("/ui")
    assert "/static/" not in resp.text


async def test_ui_contains_polling_js(ui_client):
    """UI-02: HTML contiene setInterval con fetch a /health para polling."""
    resp = await ui_client.get("/ui")
    assert "setInterval" in resp.text
    assert "/health" in resp.text


async def test_ui_contains_all_config_fields(ui_client):
    """UI-03: HTML contiene inputs para todos los campos de AppConfig."""
    resp = await ui_client.get("/ui")
    html = resp.text
    # Rembg
    assert "rembg-model" in html
    assert "rembg-alpha-matting" in html
    # Output
    assert "output-size" in html
    assert "output-quality" in html
    assert "output-bg-color" in html
    # Padding
    assert "padding-enabled" in html
    assert "padding-percent" in html
    # Autocrop
    assert "autocrop-enabled" in html
    assert "autocrop-threshold" in html
    # Enhancement
    assert "enhancement-brightness" in html
    assert "enhancement-contrast" in html
    # Queue
    assert "queue-max-concurrent" in html
    assert "queue-max-queue-size" in html
    assert "queue-timeout" in html


async def test_ui_contains_save_button(ui_client):
    """UI-04: Boton guardar presente en HTML."""
    resp = await ui_client.get("/ui")
    assert "save-btn" in resp.text
    assert "saveConfig" in resp.text


async def test_ui_contains_restore_button(ui_client):
    """UI-04: Boton restaurar defaults presente en HTML."""
    resp = await ui_client.get("/ui")
    assert "restore-btn" in resp.text
    assert "restoreDefaults" in resp.text


async def test_ui_contains_yaml_button(ui_client):
    """UI-04: Boton ver YAML presente en HTML."""
    resp = await ui_client.get("/ui")
    assert "yaml-btn" in resp.text or "yaml-view" in resp.text


async def test_ui_contains_dark_mode_css(ui_client):
    """UI-05: HTML contiene prefers-color-scheme en CSS."""
    resp = await ui_client.get("/ui")
    assert "prefers-color-scheme" in resp.text


async def test_ui_mobile_meta_tag(ui_client):
    """UI-05: HTML contiene viewport meta tag (mobile-friendly)."""
    resp = await ui_client.get("/ui")
    assert "viewport" in resp.text
    assert "width=device-width" in resp.text


async def test_ui_contains_valid_models(ui_client):
    """UI-03: El dropdown de modelos contiene las opciones de VALID_MODELS."""
    resp = await ui_client.get("/ui")
    assert "isnet-general-use" in resp.text  # default model debe estar


async def test_health_includes_model_swapping(ui_client):
    """Pitfall 1: GET /health incluye model_swapping en la respuesta."""
    resp = await ui_client.get("/health")
    data = resp.json()
    assert "model_swapping" in data
    assert data["model_swapping"] is False
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -m pytest tests/test_ui.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ui.py contiene `test_ui_returns_html`
    - tests/test_ui.py contiene `test_ui_no_static_references`
    - tests/test_ui.py contiene `test_ui_contains_polling_js`
    - tests/test_ui.py contiene `test_ui_contains_all_config_fields`
    - tests/test_ui.py contiene `test_ui_contains_save_button`
    - tests/test_ui.py contiene `test_ui_contains_restore_button`
    - tests/test_ui.py contiene `test_ui_contains_dark_mode_css`
    - tests/test_ui.py contiene `test_ui_mobile_meta_tag`
    - tests/test_ui.py contiene `test_health_includes_model_swapping`
    - `pytest tests/test_ui.py -x -q` sale con exit code 0 (todos los tests pasan)
  </acceptance_criteria>
  <done>11 tests en test_ui.py pasan verificando UI-01 a UI-05 + model_swapping en /health. Todos los tests usan el patron de fixture con lifespan mock.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_ui.py -x -q` — 11 tests pasan
2. `python -m pytest tests/ -x -q` — suite completa sigue verde (no se rompio nada)
3. `python -c "from app.main import app; routes=[r.path for r in app.routes]; assert '/ui' in routes"` — ruta registrada
4. `grep "model_swapping" app/router_api.py` — campo agregado al health endpoint
</verification>

<success_criteria>
- GET /ui retorna 200 con text/html que contiene todos los controles de AppConfig (verificado por tests)
- GET /health retorna model_swapping: false en su JSON (verificado por test)
- El template HTML stub es funcional (tiene JS de polling, save, restore, YAML view)
- 11 tests nuevos pasan en test_ui.py
- Suite completa de tests sigue verde
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui-de-configuracion/04-01-SUMMARY.md`
</output>
