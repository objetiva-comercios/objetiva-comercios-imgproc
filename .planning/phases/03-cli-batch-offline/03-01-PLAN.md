---
phase: 03-cli-batch-offline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cli.py
  - app/cli.py
  - pyproject.toml
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05

must_haves:
  truths:
    - "imgproc process foto.jpg produce un WebP en el directorio de salida"
    - "imgproc batch ./fotos/ procesa todos los archivos soportados y genera reporte CSV"
    - "imgproc serve arranca Uvicorn con host/port/log_level de la config"
    - "imgproc config show muestra el YAML de settings.yaml"
    - "imgproc config set output.quality 95 persiste el cambio en disco"
    - "El CLI no duplica logica de pipeline — llama process_image() directo"
    - "rembg se inicializa lazy solo para process y batch, no para serve ni config"
  artifacts:
    - path: "app/cli.py"
      provides: "CLI Typer app con todos los subcomandos"
      min_lines: 150
    - path: "tests/test_cli.py"
      provides: "Tests unitarios para todos los comandos CLI"
      min_lines: 100
    - path: "pyproject.toml"
      provides: "Entry point imgproc = app.cli:app"
      contains: "imgproc"
  key_links:
    - from: "app/cli.py"
      to: "app/processor.py"
      via: "from app.processor import process_image, ProcessingError"
      pattern: "from app\\.processor import process_image"
    - from: "app/cli.py"
      to: "app/config.py"
      via: "from app.config import ConfigManager"
      pattern: "from app\\.config import ConfigManager"
    - from: "app/cli.py"
      to: "app/models.py"
      via: "from app.models import AppConfig"
      pattern: "from app\\.models import AppConfig"
    - from: "tests/test_cli.py"
      to: "app/cli.py"
      via: "from app.cli import app + CliRunner"
      pattern: "from app\\.cli import app"
---

<objective>
Implementar la interfaz CLI completa del Image Standardizer usando Typer, con comandos process, batch, serve, config show y config set. El CLI reutiliza directamente process_image() sin duplicar logica de pipeline.

Purpose: El operador puede procesar imagenes individuales o directorios completos desde la terminal sin levantar el servidor HTTP.
Output: app/cli.py funcional, tests/test_cli.py con cobertura de todos los comandos, pyproject.toml con entry point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-batch-offline/03-CONTEXT.md
@.planning/phases/03-cli-batch-offline/03-RESEARCH.md

@app/processor.py
@app/config.py
@app/models.py
@pyproject.toml
@tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/processor.py:
```python
def process_image(
    image_bytes: bytes,
    article_id: str,
    config: AppConfig,
    rembg_session,
) -> ProcessingResult:
    """Pipeline completo sincrono. CPU-bound."""

class ProcessingError(Exception):
    def __init__(self, step: str, detail: str, article_id: str | None = None):
```

From app/config.py:
```python
class ConfigManager:
    def __init__(self, config_path: str = "config/settings.yaml"):
    def reload(self) -> None:
    @property
    def config(self) -> AppConfig:
    def get_snapshot(self) -> AppConfig:
    def update_config(self, new_config: AppConfig) -> None:
```

From app/models.py:
```python
class AppConfig(BaseModel):
    rembg: RembgConfig = RembgConfig()
    output: OutputConfig = OutputConfig()
    padding: PaddingConfig = PaddingConfig()
    autocrop: AutocropConfig = AutocropConfig()
    enhancement: EnhancementConfig = EnhancementConfig()
    queue: QueueConfig = QueueConfig()
    server: ServerConfig = ServerConfig()

class ServerConfig(BaseModel):
    host: str = "0.0.0.0"
    port: int = 8010
    log_level: str = "info"

class ProcessingResult(BaseModel):
    image_bytes: bytes
    article_id: str
    processing_time_ms: int
    model_used: str
    original_size: str
    output_size: str
    steps_applied: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests CLI — test_cli.py con cobertura de todos los comandos</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - app/processor.py (firma de process_image y ProcessingError)
    - app/config.py (interfaz de ConfigManager, update_config)
    - app/models.py (AppConfig, ProcessingResult, ServerConfig)
    - tests/conftest.py (fixtures existentes)
    - .planning/phases/03-cli-batch-offline/03-RESEARCH.md (patrones de test con CliRunner)
  </read_first>
  <behavior>
    - test_process_command: invoke ["process", str(input_file), "-o", str(output_dir)] con process_image mockeado retorna exit_code 0, crea archivo {stem}.webp en output_dir
    - test_process_with_article_id: invoke ["process", str(input_file), "--article-id", "ART-001", "-o", str(output_dir)] pasa article_id="ART-001" a process_image
    - test_process_default_article_id: sin --article-id, pasa el stem del archivo como article_id
    - test_process_invalid_image: process_image lanza ProcessingError(step="decode", detail="corrupt") -> exit_code 1, stderr contiene "Error"
    - test_process_nonexistent_file: invoke con archivo inexistente -> exit_code != 0
    - test_process_creates_output_dir: output dir no existe, se crea automaticamente
    - test_batch_command: directorio con 2 archivos jpg -> procesa ambos, crea 2 webp en output
    - test_batch_report_csv: invoke con --report genera CSV con columnas article_id,input_path,output_path,status,processing_time_ms,error
    - test_batch_continues_on_error: un archivo falla (ProcessingError), el otro se procesa ok, CSV tiene status "error" y "ok"
    - test_batch_empty_directory: directorio sin imagenes -> exit_code 0, mensaje "No se encontraron"
    - test_batch_not_a_directory: path es un archivo -> exit_code 1
    - test_serve_command: invoke ["serve"] mockea uvicorn.run, verifica llamada con host, port, log_level del config
    - test_config_show: invoke ["config", "show"] muestra contenido del settings.yaml
    - test_config_set_valid: invoke ["config", "set", "output.quality", "95"] llama update_config, exit_code 0
    - test_config_set_invalid: invoke ["config", "set", "output.quality", "abc"] -> exit_code 1, stderr contiene "Error"
  </behavior>
  <action>
    Crear tests/test_cli.py con todos los tests listados en behavior.

    Imports necesarios:
    ```python
    from typer.testing import CliRunner
    from unittest.mock import patch, MagicMock, PropertyMock
    from pathlib import Path
    from app.cli import app
    from app.processor import ProcessingError
    ```

    Runner: `runner = CliRunner()` a nivel modulo.

    Mock targets (IMPORTANTE — estos son los paths de import en cli.py, no en el modulo original):
    - `app.cli.process_image` — retorna MagicMock con image_bytes=b"fake_webp", processing_time_ms=100, article_id="test"
    - `app.cli._get_rembg_session` — retorna MagicMock() (evita cargar modelo ONNX)
    - `app.cli.ConfigManager` — mock con config property retornando AppConfig() defaults, get_snapshot() retornando AppConfig(), update_config() como MagicMock
    - `uvicorn.run` — mock para test_serve_command

    Para tests de filesystem (process, batch): usar fixture `tmp_path` de pytest.
    Crear archivos de entrada con `(tmp_path / "foto.jpg").write_bytes(b"fake_image_data")`.

    Para test_config_show: mockear ConfigManager para que _config_path apunte a un tmp file con contenido YAML conocido. Alternativamente, mockear `open()` o crear un settings.yaml temporal.

    Para test_config_set_valid: verificar que update_config fue llamado con un AppConfig donde output.quality == 95 (Pydantic coerce string "95" a int).

    Para test_batch_report_csv: leer el CSV generado con csv.DictReader y verificar fieldnames == ["article_id", "input_path", "output_path", "status", "processing_time_ms", "error"].

    Cada test DEBE ser independiente — no compartir estado entre tests.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -m pytest tests/test_cli.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cli.py existe y contiene al menos 15 funciones test_*
    - tests/test_cli.py contiene `from typer.testing import CliRunner`
    - tests/test_cli.py contiene `from app.cli import app`
    - tests/test_cli.py contiene `def test_process_command`
    - tests/test_cli.py contiene `def test_batch_command`
    - tests/test_cli.py contiene `def test_batch_report_csv`
    - tests/test_cli.py contiene `def test_batch_continues_on_error`
    - tests/test_cli.py contiene `def test_serve_command`
    - tests/test_cli.py contiene `def test_config_show`
    - tests/test_cli.py contiene `def test_config_set_valid`
    - tests/test_cli.py contiene `def test_config_set_invalid`
    - Todos los tests fallan (RED phase) porque app/cli.py no existe aun
  </acceptance_criteria>
  <done>tests/test_cli.py creado con 15+ tests cubriendo CLI-01 a CLI-04, todos fallan porque app/cli.py no existe todavia</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar app/cli.py + entry point pyproject.toml (GREEN phase)</name>
  <files>app/cli.py, pyproject.toml</files>
  <read_first>
    - tests/test_cli.py (tests que deben pasar — este es el contrato)
    - app/processor.py (firma de process_image, ProcessingError)
    - app/config.py (ConfigManager completo — constructor, get_snapshot, update_config, config property)
    - app/models.py (AppConfig con todos los sub-models, ProcessingResult)
    - pyproject.toml (estado actual)
    - .planning/phases/03-cli-batch-offline/03-CONTEXT.md (decisiones D-01 a D-12)
    - .planning/phases/03-cli-batch-offline/03-RESEARCH.md (patterns 1-8, anti-patterns, pitfalls)
  </read_first>
  <action>
    Crear app/cli.py con la siguiente estructura exacta:

    **Imports:**
    ```python
    import csv
    import time
    from pathlib import Path
    from typing import Optional

    import typer
    import yaml
    from rich.progress import Progress, BarColumn, TextColumn, TimeElapsedColumn, MofNCompleteColumn

    from app.config import ConfigManager
    from app.models import AppConfig
    from app.processor import process_image, ProcessingError
    ```

    **Constantes:**
    ```python
    EXTENSIONES_SOPORTADAS = {".jpg", ".jpeg", ".png", ".webp", ".bmp", ".tiff", ".tif"}
    ```

    **Typer apps (per D-09):**
    ```python
    app = typer.Typer(name="imgproc", help="Image Standardizer CLI")
    config_app = typer.Typer(help="Gestionar configuracion")
    ```

    **Lazy rembg init (per D-01):**
    Variable modulo-level `_rembg_session = None`. Funcion `_get_rembg_session(model: str)` que hace `global _rembg_session`, importa rembg dentro de la funcion, llama `rembg.new_session(model)` solo si es None, retorna la sesion. Mostrar `typer.echo(f"Cargando modelo {model}...")` antes de cargar.

    **Helpers:**
    - `_dotpath_to_nested(path: str, value: str) -> dict`: split por ".", construir dict anidado con el valor como hoja. Iterar `reversed(keys)` envolviendo en dicts.
    - `_deep_merge(base: dict, override: dict) -> dict`: merge recursivo donde override sobreescribe base, mergeando dicts anidados.
    - `_escribir_reporte(rows: list[dict], report_path: Path) -> None`: csv.DictWriter con fieldnames=["article_id", "input_path", "output_path", "status", "processing_time_ms", "error"], writeheader + writerows.

    **Comando process (per D-11, CLI-01, CLI-05):**
    ```python
    @app.command()
    def process(
        image: Path = typer.Argument(..., help="Imagen a procesar (JPG, PNG, WebP, BMP, TIFF)"),
        output: Path = typer.Option(Path("./output"), "-o", "--output", help="Directorio de salida"),
        article_id: str = typer.Option("", "--article-id", "-a", help="ID del articulo (default: nombre del archivo)"),
    ):
    ```
    - Validar `image.is_file()`, si no: secho rojo "Error: '{image}' no es un archivo" a stderr, raise typer.Exit(1)
    - Si article_id vacio: `article_id = image.stem` (Claude's Discretion)
    - `output.mkdir(parents=True, exist_ok=True)` (per D-03)
    - Instanciar ConfigManager(), get_snapshot()
    - Llamar `_get_rembg_session(config.rembg.model)` (per D-01)
    - Leer `image.read_bytes()`
    - Llamar `process_image(image_bytes, article_id, config, session)` directo sincrono (per D-11)
    - En except ProcessingError: secho rojo "Error [{e.step}]: {e.detail}" a stderr, raise typer.Exit(1)
    - Guardar `(output / f"{image.stem}.webp").write_bytes(result.image_bytes)` (per D-04)
    - secho verde con article_id, processing_time_ms, output_path

    **Comando batch (per D-12, D-06, D-05, CLI-02):**
    ```python
    @app.command()
    def batch(
        directory: Path = typer.Argument(..., help="Directorio con imagenes a procesar"),
        output: Path = typer.Option(Path("./output"), "-o", "--output", help="Directorio de salida"),
        report: Optional[Path] = typer.Option(None, "-r", "--report", help="Path del CSV de reporte"),
    ):
    ```
    - Validar `directory.is_dir()`, si no: secho rojo, Exit(1) (Pitfall 6)
    - Listar archivos: `sorted(f for f in directory.iterdir() if f.suffix.lower() in EXTENSIONES_SOPORTADAS)`
    - Si lista vacia: secho amarillo "No se encontraron imagenes en el directorio", Exit(0)
    - `output.mkdir(parents=True, exist_ok=True)` (per D-03)
    - ConfigManager(), get_snapshot() UNA vez (Pitfall 3 — no re-instanciar por archivo)
    - `_get_rembg_session(config.rembg.model)` UNA vez antes del loop (Pitfall 2)
    - rows = []
    - Rich Progress con TextColumn, BarColumn, MofNCompleteColumn, TimeElapsedColumn (per D-06)
    - Loop secuencial (per D-12): para cada archivo, construir row dict, try process_image, catch ProcessingError y Exception generica, registrar status "ok"/"error" en row, append a rows, progress.advance
    - Al final: echo resumen "N/total imagenes procesadas correctamente"
    - Si report: llamar `_escribir_reporte(rows, report)`, secho cyan

    **Comando serve (per CLI-03):**
    ```python
    @app.command()
    def serve():
        """Inicia el servidor HTTP con Uvicorn."""
        cfg = ConfigManager().config
        import uvicorn
        uvicorn.run(
            "app.main:app",
            host=cfg.server.host,
            port=cfg.server.port,
            log_level=cfg.server.log_level,
        )
    ```

    **Sub-app config (per D-07, D-08, CLI-04):**
    ```python
    @config_app.command("show")
    def config_show():
        """Muestra la configuracion activa (YAML)."""
        cfg_manager = ConfigManager()
        path = cfg_manager._config_path
        if path.exists():
            typer.echo(path.read_text())
        else:
            typer.echo(yaml.dump(cfg_manager.config.model_dump(), default_flow_style=False, sort_keys=False))

    @config_app.command("set")
    def config_set(
        key: str = typer.Argument(..., help="Clave en dotpath (ej: output.quality)"),
        value: str = typer.Argument(..., help="Valor a asignar"),
    ):
        """Modifica un valor de configuracion usando dotpath notation."""
        cfg_manager = ConfigManager()
        current = cfg_manager.config.model_dump()
        override = _dotpath_to_nested(key, value)
        merged = _deep_merge(current, override)
        try:
            new_config = AppConfig(**merged)
        except Exception as e:
            typer.secho(f"Error: valor invalido para '{key}': {e}", fg=typer.colors.RED, err=True)
            raise typer.Exit(1)
        cfg_manager.update_config(new_config)
        typer.secho(f"OK: {key} = {value}", fg=typer.colors.GREEN)
    ```

    **Registrar sub-app DESPUES de definir sus comandos (Pitfall 5):**
    ```python
    app.add_typer(config_app, name="config")
    ```

    **Entry point (per D-10):**
    ```python
    if __name__ == "__main__":
        app()
    ```

    **Actualizar pyproject.toml:** Agregar seccion `[project.scripts]` con:
    ```toml
    [project.scripts]
    imgproc = "app.cli:app"
    ```
    Insertar despues de la linea `description = ...` y antes de `[tool.pytest.ini_options]`.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -m pytest tests/test_cli.py -x --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - app/cli.py existe y contiene `app = typer.Typer(name="imgproc"`
    - app/cli.py contiene `config_app = typer.Typer(`
    - app/cli.py contiene `app.add_typer(config_app, name="config")`
    - app/cli.py contiene `from app.processor import process_image, ProcessingError`
    - app/cli.py contiene `from app.config import ConfigManager`
    - app/cli.py contiene `from app.models import AppConfig`
    - app/cli.py contiene `_rembg_session = None`
    - app/cli.py contiene `def _get_rembg_session(`
    - app/cli.py contiene `def process(`
    - app/cli.py contiene `def batch(`
    - app/cli.py contiene `def serve(`
    - app/cli.py contiene `def config_show(`
    - app/cli.py contiene `def config_set(`
    - app/cli.py contiene `def _dotpath_to_nested(`
    - app/cli.py contiene `def _deep_merge(`
    - app/cli.py contiene `def _escribir_reporte(`
    - app/cli.py contiene `EXTENSIONES_SOPORTADAS`
    - app/cli.py contiene `if __name__ == "__main__":`
    - pyproject.toml contiene `imgproc = "app.cli:app"`
    - `python -m pytest tests/test_cli.py -x` sale con exit code 0 (todos los tests GREEN)
    - `python -c "from app.cli import app; print('OK')"` imprime OK sin error
  </acceptance_criteria>
  <done>app/cli.py implementado con todos los comandos, pyproject.toml con entry point, todos los tests de test_cli.py pasan en verde</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_cli.py -x --tb=short` — todos los tests CLI pasan
2. `python -m pytest tests/ -x --tb=short` — suite completa sigue pasando (no rompe tests existentes)
3. `python -c "from app.cli import app; print(type(app))"` — importa sin error
4. `grep -c "def test_" tests/test_cli.py` — al menos 15 funciones de test
5. `grep "from app.processor import process_image" app/cli.py` — reutiliza el processor (CLI-05)
6. `grep "_rembg_session = None" app/cli.py` — lazy init presente (D-01)
7. `grep 'imgproc = "app.cli:app"' pyproject.toml` — entry point registrado (D-10)
</verification>

<success_criteria>
- Todos los tests en tests/test_cli.py pasan (15+ tests)
- Suite completa de tests existentes sigue en verde
- app/cli.py tiene todos los comandos: process, batch, serve, config show, config set
- El CLI no importa rembg a nivel de modulo (lazy init)
- El CLI no duplica logica de pipeline (import directo de process_image)
- pyproject.toml tiene entry point para imgproc
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-batch-offline/03-01-SUMMARY.md`
</output>
