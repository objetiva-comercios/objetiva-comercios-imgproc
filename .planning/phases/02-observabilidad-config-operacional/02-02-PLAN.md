---
phase: 02-observabilidad-config-operacional
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/main.py
  - app/router_config.py
  - app/router_api.py
  - tests/test_watchdog.py
autonomous: true
requirements: [CONF-04, CONF-05]

must_haves:
  truths:
    - "Editar settings.yaml en disco recarga la configuracion automaticamente sin restart"
    - "POST /config escribe el YAML sin que watchdog genere un reload duplicado"
    - "Si se cambia el modelo rembg via POST /config, la sesion ONNX se recrea gracefully"
    - "Durante el swap de modelo, requests entrantes reciben 503"
    - "Si la carga del nuevo modelo falla, la sesion vieja sigue funcionando"
  artifacts:
    - path: "app/main.py"
      provides: "Watchdog Observer en lifespan + ConfigReloadHandler + _swap_rembg_session"
      contains: "ConfigReloadHandler"
    - path: "app/router_config.py"
      provides: "POST /config dispara model swap cuando rembg.model cambia"
      contains: "model_swapping"
    - path: "app/router_api.py"
      provides: "POST /process rechaza con 503 durante model swap"
      contains: "model_swapping"
    - path: "tests/test_watchdog.py"
      provides: "Tests de watchdog reload y model swap"
      min_lines: 60
  key_links:
    - from: "app/main.py ConfigReloadHandler.on_modified"
      to: "app/main.py _reload_config"
      via: "asyncio.run_coroutine_threadsafe"
      pattern: "run_coroutine_threadsafe"
    - from: "app/router_config.py POST /config"
      to: "app/main.py _swap_rmbg_session"
      via: "asyncio.create_task cuando modelo cambia"
      pattern: "create_task.*swap"
    - from: "app/router_api.py POST /process"
      to: "app.state.model_swapping"
      via: "check de flag antes de encolar"
      pattern: "model_swapping"
    - from: "app/main.py watchdog suppress_flag"
      to: "app/router_config.py POST /config"
      via: "suppress_flag.set() antes de escribir YAML"
      pattern: "suppress_flag\\.set"
---

<objective>
Agregar hot-reload de config via watchdog y swap graceful de sesion rembg al cambiar modelo.

Purpose: El operador puede editar el YAML en el host y el servicio recarga sin restart. Si cambia el modelo, el swap es atomico y seguro.
Output: Watchdog Observer corriendo en el lifespan, model swap con bloqueo de cola, flag de supresion para evitar double-reload, y tests completos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observabilidad-config-operacional/02-CONTEXT.md
@.planning/phases/02-observabilidad-config-operacional/02-RESEARCH.md
@.planning/phases/02-observabilidad-config-operacional/02-01-SUMMARY.md

<interfaces>
<!-- Interfaces de Plan 01 que este plan consume -->

From app/config.py (despues de Plan 01):
```python
class ConfigManager:
    def __init__(self, config_path: str = "config/settings.yaml"):
    def _load(self) -> AppConfig:
    def reload(self) -> None:
    @property
    def config(self) -> AppConfig:
    def get_snapshot(self) -> AppConfig:
    def update_config(self, new_config: AppConfig) -> None:  # NUEVO en Plan 01
```

From app/router_config.py (despues de Plan 01):
```python
router = APIRouter()

@router.get("/config")
async def get_config(request: Request): ...

@router.post("/config")
async def update_config(request: Request, body: dict): ...

@router.get("/status")
async def status_endpoint(request: Request): ...
```

From app/main.py (estado actual):
```python
# app.state tiene: config_manager, rembg_session, model_loaded, model_name, job_queue, startup_time
@asynccontextmanager
async def lifespan(app: FastAPI):
    # 1. Config, 2. rembg session, 3. JobQueue, 4. startup_time
    yield
    # Shutdown

app = FastAPI(title="Image Standardizer", version="1.0.0", lifespan=lifespan)
```

From app/queue.py:
```python
class JobQueue:
    _semaphore: asyncio.Semaphore  # accesible para barrier del model swap
```

From app/models.py:
```python
class ErrorResponse(BaseModel):
    error: str
    detail: str
    article_id: Optional[str] = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Watchdog Observer + model swap + flag supresion + 503 durante swap</name>
  <files>app/main.py, app/router_config.py, app/router_api.py</files>
  <read_first>app/main.py, app/router_config.py, app/router_api.py, app/queue.py, app/config.py, app/models.py</read_first>
  <action>
**1. Agregar watchdog Observer y model swap al lifespan en app/main.py:**

Imports nuevos al inicio del archivo:
```python
import asyncio
import threading
from pathlib import Path
from watchdog.observers import Observer
from watchdog.events import FileSystemEventHandler
```

Definir ConfigReloadHandler como clase dentro del modulo (no dentro del lifespan):
```python
class ConfigReloadHandler(FileSystemEventHandler):
    """Detecta cambios en settings.yaml y recarga config via bridge asyncio. Per CONF-05."""

    def __init__(self, loop: asyncio.AbstractEventLoop, app: FastAPI, suppress_flag: threading.Event):
        self._loop = loop
        self._app = app
        self._suppress_flag = suppress_flag

    def on_modified(self, event):
        if not event.src_path.endswith("settings.yaml"):
            return
        # D-07: si el flag de supresion esta activo, ignorar este evento (fue escrito por POST /config)
        if self._suppress_flag.is_set():
            self._suppress_flag.clear()
            logger.info(json.dumps({
                "level": "info",
                "event": "watchdog_suppressed",
                "detail": "Ignoring filesystem event — config was written by API",
            }))
            return
        # Bridge thread → asyncio event loop
        asyncio.run_coroutine_threadsafe(
            _reload_config(self._app),
            self._loop,
        )
```

Definir _reload_config como funcion async del modulo:
```python
async def _reload_config(app: FastAPI) -> None:
    """Recarga config desde YAML. Se ejecuta en el event loop. Per D-08."""
    try:
        app.state.config_manager.reload()
        new_model = app.state.config_manager.config.rembg.model
        old_model = app.state.model_name

        logger.info(json.dumps({
            "level": "info",
            "event": "config_reloaded",
            "source": "watchdog",
            "model": new_model,
        }))

        # Si el modelo cambio via edicion del YAML, disparar swap
        if old_model != new_model:
            asyncio.create_task(_swap_rembg_session(app, new_model))
    except Exception as e:
        logger.error(json.dumps({
            "level": "error",
            "event": "config_reload_failed",
            "source": "watchdog",
            "error": str(e),
        }))
```

Definir _swap_rembg_session como funcion async del modulo (per CONF-04, D-01, D-02):
```python
async def _swap_rembg_session(app: FastAPI, new_model: str) -> None:
    """
    Swap graceful de sesion rembg.
    D-01: app.state.model_swapping = True bloquea cola con 503.
    D-02: Si new_session falla, sesion vieja sigue activa.
    """
    app.state.model_swapping = True
    try:
        # Esperar que termine el job activo usando el semaphore como barrier
        queue = app.state.job_queue
        timeout = app.state.config_manager.config.queue.timeout_seconds + 10
        try:
            await asyncio.wait_for(queue._semaphore.acquire(), timeout=timeout)
            queue._semaphore.release()
        except asyncio.TimeoutError:
            logger.error(json.dumps({
                "level": "error",
                "event": "model_swap_timeout",
                "new_model": new_model,
                "detail": f"Waited {timeout}s for active job to finish",
            }))
            return  # Abortar swap — sesion vieja sigue activa (D-02)

        # Cargar nueva sesion en thread (CPU-bound — per constraint asyncio.to_thread)
        new_sess = await asyncio.to_thread(new_session, new_model)

        # Swap atomico de referencia
        app.state.rembg_session = new_sess
        app.state.model_name = new_model

        logger.info(json.dumps({
            "level": "info",
            "event": "model_swapped",
            "new_model": new_model,
        }))
    except Exception as e:
        # D-02: no modificar rembg_session — sesion vieja sigue activa
        logger.error(json.dumps({
            "level": "error",
            "event": "model_swap_failed",
            "new_model": new_model,
            "error": str(e),
        }))
    finally:
        app.state.model_swapping = False
```

Modificar el lifespan para arrancar watchdog Observer despues del paso 4 (startup_time):
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # ... pasos 1-4 existentes sin cambios ...

    # 5. Watchdog para hot-reload de config (CONF-05)
    loop = asyncio.get_running_loop()  # Python 3.12+ — no usar get_event_loop()
    suppress_flag = threading.Event()
    app.state.watchdog_suppress_flag = suppress_flag
    app.state.model_swapping = False

    config_dir = str(Path(config_manager._config_path).parent)
    handler = ConfigReloadHandler(loop, app, suppress_flag)
    observer = Observer()
    observer.schedule(handler, path=config_dir, recursive=False)
    observer.start()
    app.state.watchdog_observer = observer

    logger.info(json.dumps({
        "level": "info",
        "event": "watchdog_started",
        "watch_path": config_dir,
    }))

    yield

    # Shutdown — detener watchdog primero
    observer.stop()
    observer.join()
    logger.info(json.dumps({"level": "info", "event": "shutdown"}))
```

**2. Modificar POST /config en app/router_config.py para disparar model swap:**

Despues de `config_manager.update_config(new_config)`, agregar:
```python
import asyncio

# Antes de escribir YAML: activar flag de supresion (D-07)
suppress_flag = getattr(request.app.state, "watchdog_suppress_flag", None)
if suppress_flag:
    suppress_flag.set()

config_manager.update_config(new_config)

# Si el modelo cambio, disparar swap de sesion ONNX (CONF-04)
old_model = request.app.state.model_name
new_model = new_config.rembg.model
if old_model != new_model:
    from app.main import _swap_rembg_session
    asyncio.create_task(_swap_rembg_session(request.app, new_model))
```

El log structured JSON (D-08) ya deberia estar del Plan 01. Asegurar que incluye `"model_changed": old_model != new_model`.

**3. Agregar check de model_swapping en POST /process en app/router_api.py (D-01):**

Al inicio del endpoint `process_endpoint`, despues de obtener queue/config_manager/rembg_session y ANTES de tomar config_snapshot:
```python
# D-01: Rechazar requests durante swap de modelo
if getattr(request.app.state, "model_swapping", False):
    return JSONResponse(
        status_code=503,
        content=ErrorResponse(
            error="model_swapping",
            detail="Model swap in progress, retry in a few seconds",
            article_id=article_id,
        ).model_dump(),
    )
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -c "from app.main import ConfigReloadHandler, _reload_config, _swap_rembg_session; print('Imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py contiene `class ConfigReloadHandler(FileSystemEventHandler)` con metodo `on_modified`
    - app/main.py contiene `async def _reload_config(app: FastAPI)` con log `"source": "watchdog"`
    - app/main.py contiene `async def _swap_rembg_session(app: FastAPI, new_model: str)` con `app.state.model_swapping = True` y `finally: app.state.model_swapping = False`
    - app/main.py lifespan contiene `asyncio.get_running_loop()` (no get_event_loop)
    - app/main.py lifespan contiene `observer = Observer()` y `observer.schedule(handler, path=config_dir`
    - app/main.py lifespan yield es seguido de `observer.stop()` y `observer.join()`
    - app/main.py lifespan contiene `app.state.watchdog_suppress_flag = suppress_flag` y `app.state.model_swapping = False`
    - app/router_config.py POST /config contiene `suppress_flag.set()` antes de `update_config`
    - app/router_config.py POST /config contiene `asyncio.create_task(_swap_rembg_session` cuando modelo cambia
    - app/router_api.py process_endpoint contiene `if getattr(request.app.state, "model_swapping", False)` retornando 503
    - _swap_rembg_session usa `await asyncio.to_thread(new_session, new_model)` (no directo en event loop)
    - _swap_rembg_session usa semaphore acquire/release como barrier para esperar job activo
    - ConfigReloadHandler.on_modified verifica `suppress_flag.is_set()` y llama `suppress_flag.clear()` si activo
  </acceptance_criteria>
  <done>Watchdog Observer corre en el lifespan, detecta cambios en settings.yaml, recarga config via bridge asyncio, dispara model swap cuando el modelo cambia, y POST /process rechaza con 503 durante el swap</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests de watchdog reload, supresion, y model swap</name>
  <files>tests/test_watchdog.py</files>
  <read_first>app/main.py, app/router_config.py, app/router_api.py, tests/conftest.py, tests/test_config_router.py</read_first>
  <behavior>
    - test_watchdog_reload: Escribir YAML modificado en disco, verificar que config se recarga automaticamente (polling con sleep breve)
    - test_no_double_reload_after_post: POST /config escribe YAML, verificar que watchdog NO dispara un reload adicional (flag de supresion funciona)
    - test_model_swap_sets_flag: Simular cambio de modelo, verificar que model_swapping se setea a True y vuelve a False
    - test_process_503_during_swap: Con model_swapping=True, POST /process retorna 503 con error "model_swapping"
    - test_model_swap_failure_keeps_old_session: Si new_session() falla, la sesion vieja sigue en app.state.rembg_session (D-02)
  </behavior>
  <action>
Crear tests/test_watchdog.py.

**Setup comun:** Fixture con app de test que incluye watchdog Observer real apuntando a un tmp_path:

```python
import asyncio
import pytest
import time
import yaml
import threading
from unittest.mock import AsyncMock, MagicMock, patch
from pathlib import Path
from contextlib import asynccontextmanager
from httpx import AsyncClient, ASGITransport
from fastapi import FastAPI

from app.config import ConfigManager
from app.queue import JobQueue
from app.router_config import router as config_router
from app.router_api import router as api_router
from app.main import ConfigReloadHandler, _reload_config, _swap_rembg_session
from watchdog.observers import Observer
```

**Fixture con watchdog real (para test_watchdog_reload y test_no_double_reload):**
```python
@pytest.fixture
async def app_with_watchdog(tmp_path):
    settings = {
        "rembg": {"model": "isnet-general-use"},
        "output": {"size": 800, "format": "webp", "quality": 85, "background_color": [255, 255, 255]},
        "padding": {"enabled": True, "percent": 10},
        "autocrop": {"enabled": True, "threshold": 10},
        "enhancement": {"brightness": 1.0, "contrast": 1.0},
        "queue": {"max_concurrent": 1, "max_queue_size": 10, "timeout_seconds": 120},
        "server": {"host": "0.0.0.0", "port": 8010, "log_level": "info"},
    }
    yaml_path = tmp_path / "settings.yaml"
    with open(yaml_path, "w") as f:
        yaml.dump(settings, f)

    @asynccontextmanager
    async def noop_lifespan(app):
        yield

    app = FastAPI(lifespan=noop_lifespan)
    app.include_router(config_router)
    app.include_router(api_router)

    cm = ConfigManager(config_path=str(yaml_path))
    app.state.config_manager = cm
    app.state.job_queue = JobQueue()
    app.state.model_loaded = True
    app.state.model_name = "isnet-general-use"
    app.state.startup_time = time.monotonic()
    app.state.model_swapping = False

    loop = asyncio.get_running_loop()
    suppress_flag = threading.Event()
    app.state.watchdog_suppress_flag = suppress_flag

    handler = ConfigReloadHandler(loop, app, suppress_flag)
    observer = Observer()
    observer.schedule(handler, path=str(tmp_path), recursive=False)
    observer.start()
    app.state.watchdog_observer = observer

    yield app, yaml_path

    observer.stop()
    observer.join()
```

**test_watchdog_reload:**
- Modificar el YAML en disco: cambiar `quality` de 85 a 70
- Esperar hasta 2 segundos con polling cada 0.2s: `while config_manager.config.output.quality != 70`
- Assert config.output.quality == 70

**test_no_double_reload_after_post:**
- Usar `patch("app.main._reload_config")` como mock
- Hacer POST /config con `{"output": {"quality": 60}}`
- Esperar 1 segundo para que watchdog procese el evento
- Assert que el mock de _reload_config NO fue llamado (el flag de supresion lo previno)

**test_model_swap_sets_flag:**
- Mockear `rembg.new_session` para retornar un MagicMock
- Llamar `await _swap_rembg_session(app, "birefnet-general-lite")`
- Verificar que `app.state.model_swapping` es False al terminar (el finally lo resetea)
- Verificar que `app.state.model_name == "birefnet-general-lite"`

**test_process_503_during_swap:**
- Setear `app.state.model_swapping = True` manualmente
- POST /process con form data minima
- Assert status_code == 503 y response contiene `"error": "model_swapping"`

**test_model_swap_failure_keeps_old_session (D-02):**
- Guardar referencia a la sesion vieja: `old_session = app.state.rembg_session`
- Mockear `rembg.new_session` para levantar `RuntimeError("download failed")`
- Llamar `await _swap_rembg_session(app, "modelo-que-falla")`
- Assert `app.state.rembg_session is old_session` (no cambio)
- Assert `app.state.model_swapping is False` (el finally lo reseteo)
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -m pytest tests/test_watchdog.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_watchdog.py existe con al menos 5 funciones test_*
    - `pytest tests/test_watchdog.py -x` sale con exit code 0
    - test_watchdog_reload verifica que config.output.quality cambia despues de escribir YAML
    - test_no_double_reload_after_post verifica que _reload_config no se llama cuando POST /config escribe YAML
    - test_model_swap_sets_flag verifica model_swapping=False al terminar y model_name actualizado
    - test_process_503_during_swap verifica status_code==503 y "model_swapping" en response
    - test_model_swap_failure_keeps_old_session verifica que rembg_session no cambio y model_swapping=False
  </acceptance_criteria>
  <done>Todos los tests de watchdog y model swap pasan, cubriendo CONF-04 (swap graceful con 503 + fallback) y CONF-05 (hot-reload + flag de supresion)</done>
</task>

</tasks>

<verification>
```bash
cd /home/sanchez/proyectos/objetiva-comercios-imgproc
python -m pytest tests/ -x -v
```
Toda la suite pasa: tests de Fase 1 no se rompen, tests de config_router (Plan 01) no se rompen, tests de watchdog (Plan 02) pasan.
</verification>

<success_criteria>
- Editar settings.yaml recarga config automaticamente (verificable con test_watchdog_reload)
- POST /config no genera double-reload (verificable con test_no_double_reload)
- Cambiar modelo via POST /config dispara swap graceful con 503 durante el proceso
- Si new_session falla, sesion vieja sigue activa (D-02)
- POST /process retorna 503 cuando model_swapping es True
- Toda la suite de tests pasa incluyendo los tests existentes de Fase 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-observabilidad-config-operacional/02-02-SUMMARY.md`
</output>
