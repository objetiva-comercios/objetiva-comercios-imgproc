---
phase: 02-observabilidad-config-operacional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/queue.py
  - app/config.py
  - app/router_config.py
  - app/main.py
  - tests/test_config_router.py
autonomous: true
requirements: [CONF-02, CONF-03, API-06, QUEUE-05]

must_haves:
  truths:
    - "GET /config retorna la configuracion activa del servicio como JSON"
    - "POST /config con JSON parcial actualiza solo los campos indicados y persiste el YAML"
    - "POST /config con campo invalido rechaza TODO el request con 422"
    - "POST /config con modelo rembg no reconocido retorna 422 con detalle"
    - "GET /status retorna total_processed, total_errors, avg_processing_time_ms e historial de jobs"
    - "Historial de jobs incluye original_size y output_size en cada registro"
  artifacts:
    - path: "app/router_config.py"
      provides: "Endpoints GET /config, POST /config, GET /status"
      exports: ["router"]
    - path: "app/config.py"
      provides: "ConfigManager con update_config() que escribe YAML"
      contains: "def update_config"
    - path: "app/queue.py"
      provides: "JobRecord con original_size y output_size"
      contains: "original_size"
    - path: "tests/test_config_router.py"
      provides: "Tests de integracion para los 3 endpoints"
      min_lines: 80
  key_links:
    - from: "app/router_config.py"
      to: "app/config.py"
      via: "request.app.state.config_manager"
      pattern: "config_manager\\.config\\.model_dump"
    - from: "app/router_config.py"
      to: "app/queue.py"
      via: "request.app.state.job_queue.state"
      pattern: "job_queue\\.state"
    - from: "app/main.py"
      to: "app/router_config.py"
      via: "app.include_router(config_router)"
      pattern: "include_router.*config"
---

<objective>
Crear los endpoints GET /config, POST /config y GET /status, extender JobRecord con original_size/output_size, y agregar update_config() al ConfigManager.

Purpose: El operador necesita consultar y modificar la configuracion del servicio via API, y ver metricas operacionales e historial de jobs, sin reiniciar el container.
Output: Nuevo router_config.py con 3 endpoints funcionales, ConfigManager con escritura YAML, JobRecord extendido, y test suite completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observabilidad-config-operacional/02-CONTEXT.md
@.planning/phases/02-observabilidad-config-operacional/02-RESEARCH.md

<interfaces>
<!-- Interfaces existentes que el executor necesita conocer -->

From app/models.py:
```python
class AppConfig(BaseModel):
    rembg: RembgConfig = RembgConfig()
    output: OutputConfig = OutputConfig()
    padding: PaddingConfig = PaddingConfig()
    autocrop: AutocropConfig = AutocropConfig()
    enhancement: EnhancementConfig = EnhancementConfig()
    queue: QueueConfig = QueueConfig()
    server: ServerConfig = ServerConfig()

class ProcessingResult(BaseModel):
    image_bytes: bytes
    article_id: str
    processing_time_ms: int
    model_used: str
    original_size: str       # "WxH"
    output_size: str         # "WxH"
    steps_applied: list[str]

class ErrorResponse(BaseModel):
    error: str
    detail: str
    article_id: Optional[str] = None
```

From app/config.py:
```python
class ConfigManager:
    def __init__(self, config_path: str = "config/settings.yaml"):
    def _load(self) -> AppConfig:
    def reload(self) -> None:
    @property
    def config(self) -> AppConfig:
    def get_snapshot(self) -> AppConfig:
```

From app/queue.py:
```python
@dataclass
class JobRecord:
    article_id: str
    status: str                  # "completed" | "error"
    processing_time_ms: int
    model_used: str
    timestamp: str               # ISO 8601 UTC
    error: str | None = None

@dataclass
class QueueState:
    active_jobs: int = 0
    queued_jobs: int = 0
    total_processed: int = 0
    total_errors: int = 0
    job_history: deque = field(default_factory=lambda: deque(maxlen=50))

class JobQueue:
    def __init__(self, max_concurrent, max_queue_size, timeout_seconds):
    @property
    def state(self) -> QueueState:
    async def submit_job(self, process_fn, image_bytes, article_id, config_snapshot, rembg_session):
```

From app/router_api.py:
```python
def _deep_merge(base: dict, override: dict) -> None:
    """Deep merge override into base dict (in-place)."""
```

From app/main.py:
```python
# app.state tiene: config_manager, rembg_session, model_loaded, model_name, job_queue, startup_time
app = FastAPI(title="Image Standardizer", version="1.0.0", lifespan=lifespan)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender JobRecord + ConfigManager.update_config() + router_config.py</name>
  <files>app/queue.py, app/config.py, app/router_config.py, app/main.py</files>
  <read_first>app/queue.py, app/config.py, app/router_api.py, app/main.py, app/models.py</read_first>
  <action>
**1. Extender JobRecord en app/queue.py (per D-06, QUEUE-05):**

Agregar dos campos opcionales al dataclass JobRecord:
```python
@dataclass
class JobRecord:
    article_id: str
    status: str                  # "completed" | "error"
    processing_time_ms: int
    model_used: str
    timestamp: str               # ISO 8601 UTC
    original_size: str | None = None   # "WxH" — per D-06
    output_size: str | None = None     # "WxH" — per D-06
    error: str | None = None
```

Actualizar los dos puntos en `submit_job()` donde se crea JobRecord:
- Job exitoso (linea ~192): agregar `original_size=result.original_size, output_size=result.output_size`
- Job fallido (linea ~212): dejar `original_size=None, output_size=None` (el result no existe en el except)

**2. Agregar update_config() a ConfigManager en app/config.py (per CONF-03):**

```python
import yaml
from pathlib import Path
from app.models import AppConfig

class ConfigManager:
    # ... metodos existentes sin cambios ...

    def update_config(self, new_config: AppConfig) -> None:
        """Actualiza config en memoria y persiste en YAML. Per CONF-03."""
        self._config = new_config
        with open(self._config_path, "w") as f:
            yaml.dump(
                new_config.model_dump(),
                f,
                default_flow_style=False,
                allow_unicode=True,
                sort_keys=False,
            )
```

Usar `sort_keys=False` para mantener el orden natural de los campos de Pydantic.

**3. Crear app/router_config.py con 3 endpoints (per CONF-02, CONF-03, API-06):**

```python
"""Endpoints de configuracion y status del servicio.

Endpoints:
  GET  /config  — Retorna configuracion activa como JSON (CONF-02)
  POST /config  — Actualiza config con deep merge, persiste YAML (CONF-03)
  GET  /status  — Metricas operacionales e historial de jobs (API-06)
"""
import json
import logging
from fastapi import APIRouter, Request
from fastapi.responses import JSONResponse
from app.models import AppConfig
from app.router_api import _deep_merge

logger = logging.getLogger(__name__)
router = APIRouter()
```

**GET /config (CONF-02):**
```python
@router.get("/config")
async def get_config(request: Request):
    """CONF-02: Retorna la configuracion activa como JSON."""
    return request.app.state.config_manager.config.model_dump()
```

**POST /config (CONF-03, D-03, D-04):**
- Recibe body `dict` (JSON libre)
- Si `body["rembg"]["model"]` existe, validar contra whitelist:
  ```python
  try:
      from rembg.sessions import sessions_names
      VALID_MODELS = frozenset(sessions_names)
  except ImportError:
      VALID_MODELS = frozenset([
          "u2net", "u2netp", "u2net_human_seg", "u2net_cloth_seg",
          "silueta", "isnet-general-use", "isnet-anime",
          "birefnet-general", "birefnet-general-lite", "birefnet-portrait",
          "birefnet-dis", "birefnet-hrsod", "birefnet-cod", "birefnet-massive",
          "sam", "bria-rmbg", "ben2-base",
      ])
  ```
- Si modelo no esta en VALID_MODELS, retornar 422 con `{"error": "invalid_model", "detail": "Model 'xxx' not in whitelist. Valid: [...]"}`
- Deep merge: `current_dict = config_manager.config.model_dump()`, `_deep_merge(current_dict, body)` (reutilizar helper de router_api.py)
- Validacion estricta (D-03): `try: new_config = AppConfig(**current_dict)` — si falla, retornar 422 con `{"error": "validation_error", "detail": str(e)}`
- Si pasa validacion: `config_manager.update_config(new_config)`
- Guardar old_model y new_model para deteccion de cambio de modelo (sera usado en Plan 02 para el swap). Por ahora, solo loguear si cambio:
  ```python
  old_model = request.app.state.model_name
  new_model = new_config.rembg.model
  model_changed = old_model != new_model
  ```
- Log structured JSON (D-08): `{"event": "config_reloaded", "source": "api", "model": new_config.rembg.model, "model_changed": model_changed}`
- Retornar `config_manager.config.model_dump()` con status 200

**GET /status (API-06, D-05, D-06):**
```python
@router.get("/status")
async def status_endpoint(request: Request):
    queue = request.app.state.job_queue
    state = queue.state
    history = list(state.job_history)

    completed = [j for j in history if j.status == "completed"]
    avg_ms = (
        int(sum(j.processing_time_ms for j in completed) / len(completed))
        if completed else 0
    )

    return {
        "total_processed": state.total_processed,
        "total_errors": state.total_errors,
        "avg_processing_time_ms": avg_ms,
        "job_history": [
            {
                "article_id": j.article_id,
                "status": j.status,
                "processing_time_ms": j.processing_time_ms,
                "model_used": j.model_used,
                "original_size": j.original_size,
                "output_size": j.output_size,
                "timestamp": j.timestamp,
                "error": j.error,
            }
            for j in reversed(history)
        ],
    }
```

**4. Registrar router en app/main.py:**

Al final de main.py, despues del import de api_router:
```python
from app.router_config import router as config_router  # noqa: E402
app.include_router(config_router)
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -c "from app.router_config import router; from app.config import ConfigManager; from app.queue import JobRecord; j = JobRecord('a','completed',100,'m','t',original_size='100x100',output_size='800x800'); assert j.original_size == '100x100'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/router_config.py existe y contiene `@router.get("/config")`, `@router.post("/config")`, `@router.get("/status")`
    - app/config.py contiene `def update_config(self, new_config: AppConfig)`
    - app/queue.py JobRecord tiene campos `original_size: str | None = None` y `output_size: str | None = None`
    - app/queue.py submit_job exitoso pasa `original_size=result.original_size` al JobRecord
    - app/queue.py submit_job fallido deja `original_size=None, output_size=None`
    - app/main.py contiene `from app.router_config import router as config_router` y `app.include_router(config_router)`
    - POST /config valida modelo contra whitelist (contiene `VALID_MODELS` y `sessions_names`)
    - POST /config usa `_deep_merge` importado de router_api
    - POST /config retorna 422 con `"error": "validation_error"` si Pydantic falla
    - GET /status calcula `avg_processing_time_ms` sobre jobs completados
    - GET /status incluye `original_size` y `output_size` en cada item del historial
  </acceptance_criteria>
  <done>Los 3 endpoints responden correctamente, ConfigManager persiste YAML, JobRecord tiene los campos extendidos, router registrado en app</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests de integracion para GET/POST /config y GET /status</name>
  <files>tests/test_config_router.py</files>
  <read_first>app/router_config.py, app/config.py, app/queue.py, app/models.py, app/main.py, tests/conftest.py, tests/test_api.py</read_first>
  <behavior>
    - test_get_config: GET /config retorna 200 con las claves rembg, output, padding, autocrop, enhancement, queue, server
    - test_post_config_merge: POST /config con {"output": {"quality": 95}} retorna 200, config activa tiene quality=95, el resto sin cambios
    - test_post_config_persist_yaml: POST /config escribe el YAML en disco, nueva lectura del archivo tiene el valor actualizado
    - test_post_config_invalid_rejects_all: POST /config con {"output": {"quality": "no-es-int"}} retorna 422 con error "validation_error"
    - test_post_config_invalid_model: POST /config con {"rembg": {"model": "modelo-inventado"}} retorna 422 con error "invalid_model"
    - test_get_status_empty: GET /status retorna 200 con total_processed=0, total_errors=0, avg_processing_time_ms=0, job_history=[]
    - test_get_status_with_history: Inyectar JobRecords en queue.state.job_history, GET /status retorna historial correcto con original_size y output_size
    - test_status_avg_calculation: Inyectar 3 JobRecords completados con tiempos 100, 200, 300ms, verificar avg_processing_time_ms=200
  </behavior>
  <action>
Crear tests/test_config_router.py con tests de integracion usando httpx.AsyncClient + ASGITransport.

**Setup del test client:** Seguir el patron de tests/test_api.py de Fase 1. Usar fixture que:
1. Crea tmp_path con settings.yaml temporal
2. Instancia ConfigManager apuntando al YAML temporal
3. Crea app de FastAPI con lifespan mockeado (sin cargar rembg real)
4. Configura app.state con config_manager, job_queue, model_loaded, model_name, startup_time
5. Retorna AsyncClient con ASGITransport

```python
import pytest
import yaml
from collections import deque
from datetime import datetime, timezone
from httpx import AsyncClient, ASGITransport
from fastapi import FastAPI
from contextlib import asynccontextmanager

from app.config import ConfigManager
from app.queue import JobQueue, JobRecord, QueueState
from app.router_config import router as config_router
from app.router_api import router as api_router
import time


@asynccontextmanager
async def test_lifespan(app: FastAPI):
    yield


@pytest.fixture
def test_app(tmp_path):
    """App de test con ConfigManager apuntando a YAML temporal."""
    # Escribir settings.yaml default
    settings = {
        "rembg": {"model": "isnet-general-use"},
        "output": {"size": 800, "format": "webp", "quality": 85, "background_color": [255, 255, 255]},
        "padding": {"enabled": True, "percent": 10},
        "autocrop": {"enabled": True, "threshold": 10},
        "enhancement": {"brightness": 1.0, "contrast": 1.0},
        "queue": {"max_concurrent": 1, "max_queue_size": 10, "timeout_seconds": 120},
        "server": {"host": "0.0.0.0", "port": 8010, "log_level": "info"},
    }
    yaml_path = tmp_path / "settings.yaml"
    with open(yaml_path, "w") as f:
        yaml.dump(settings, f)

    app = FastAPI(lifespan=test_lifespan)
    app.include_router(config_router)

    cm = ConfigManager(config_path=str(yaml_path))
    app.state.config_manager = cm
    app.state.job_queue = JobQueue()
    app.state.model_loaded = True
    app.state.model_name = "isnet-general-use"
    app.state.startup_time = time.monotonic()

    return app
```

Cada test usa `async with AsyncClient(transport=ASGITransport(app=test_app), base_url="http://test") as client:`.

**test_post_config_persist_yaml:** Despues de POST, abrir el YAML del tmp_path y verificar que el valor cambio con `yaml.safe_load()`.

**test_get_status_with_history:** Inyectar directamente en `test_app.state.job_queue.state.job_history`:
```python
test_app.state.job_queue.state.job_history.append(JobRecord(
    article_id="art-1", status="completed", processing_time_ms=150,
    model_used="isnet-general-use", timestamp=datetime.now(timezone.utc).isoformat(),
    original_size="200x150", output_size="800x800",
))
```
Verificar que la respuesta JSON contiene `original_size` y `output_size` en el primer item del historial.

**Nota sobre mock de VALID_MODELS para test_post_config_invalid_model:**
El test debe funcionar sin rembg instalado. Si `from rembg.sessions import sessions_names` falla en CI, el fallback hardcodeado en router_config.py se activa. El test pasa un modelo inventado `"modelo-inventado"` que no esta en ninguna lista.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python -m pytest tests/test_config_router.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_config_router.py existe con al menos 8 funciones test_*
    - `pytest tests/test_config_router.py -x` sale con exit code 0
    - test_get_config verifica que response.json() tiene claves "rembg", "output", "queue"
    - test_post_config_merge verifica quality==95 en response
    - test_post_config_persist_yaml lee el YAML del disco y verifica el valor actualizado
    - test_post_config_invalid_rejects_all verifica status_code==422 y "validation_error" en response
    - test_post_config_invalid_model verifica status_code==422 y "invalid_model" en response
    - test_get_status_empty verifica total_processed==0 y job_history==[]
    - test_get_status_with_history verifica que original_size y output_size aparecen en el historial
    - test_status_avg_calculation verifica avg_processing_time_ms==200 para jobs de 100,200,300ms
  </acceptance_criteria>
  <done>Todos los tests de test_config_router.py pasan en verde, cubriendo CONF-02, CONF-03 (deep merge + validacion estricta + whitelist), API-06 (metricas + historial enriquecido), QUEUE-05 (estado en memoria expuesto)</done>
</task>

</tasks>

<verification>
```bash
cd /home/sanchez/proyectos/objetiva-comercios-imgproc
python -m pytest tests/test_config_router.py tests/test_queue.py tests/test_config.py -x -v
```
Todos los tests pasan. Los tests existentes de queue y config no se rompen.
</verification>

<success_criteria>
- GET /config retorna JSON con la config completa del servicio
- POST /config con {"output": {"quality": 95}} actualiza solo quality, persiste en YAML
- POST /config con campo invalido retorna 422 sin aplicar nada
- POST /config con modelo desconocido retorna 422 con whitelist
- GET /status retorna metricas con avg calculado e historial con original_size/output_size
- JobRecord en queue.py tiene original_size y output_size
- Todos los tests nuevos pasan, tests existentes no se rompen
</success_criteria>

<output>
After completion, create `.planning/phases/02-observabilidad-config-operacional/02-01-SUMMARY.md`
</output>
