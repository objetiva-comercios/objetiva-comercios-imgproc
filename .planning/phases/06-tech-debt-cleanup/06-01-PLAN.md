---
phase: 06-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app/processor.py
  - CLAUDE.md
  - app/templates/ui.html
  - tests/test_ui.py
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "scipy y numpy aparecen como dependencias explicitas en requirements.txt"
    - "app/templates/ui.html no hace requests a dominios externos (Google Fonts, unpkg.com)"
    - "CLAUDE.md documenta isnet-general-use como modelo activo (no birefnet-lite)"
    - "app/processor.py linea 115 usa numpy vectorizado en lugar de generator Python puro"
    - "Todos los iconos de Lucide se renderizan desde SVGs inline en el HTML"
  artifacts:
    - path: "requirements.txt"
      provides: "Dependencias scipy y numpy declaradas explicitamente"
      contains: "scipy>=1.16"
    - path: "app/processor.py"
      provides: "Conteo de pixeles transparentes con numpy"
      contains: "np.array(alpha_channel)"
    - path: "CLAUDE.md"
      provides: "Documentacion consistente con modelo isnet-general-use"
      contains: "isnet-general-use"
    - path: "app/templates/ui.html"
      provides: "HTML autocontenido sin CDN externos"
    - path: "tests/test_ui.py"
      provides: "Test que verifica ausencia de CDN externos"
      contains: "test_ui_no_external_cdn"
  key_links:
    - from: "app/processor.py"
      to: "requirements.txt"
      via: "import numpy as np at module level"
      pattern: "^import numpy"
    - from: "tests/test_ui.py"
      to: "app/templates/ui.html"
      via: "test verifica ausencia de CDN en HTML renderizado"
      pattern: "fonts.googleapis.com.*not in"
---

<objective>
Cerrar los 4 items de tech debt identificados en el milestone audit v1.0: declarar dependencias transitivas (TECH-DEBT-01), eliminar CDN externos de la UI (TECH-DEBT-02), corregir documentacion de modelo en CLAUDE.md (TECH-DEBT-03), y migrar conteo de pixeles transparentes a numpy (TECH-DEBT-04).

Purpose: El servicio opera correctamente pero tiene riesgos latentes (dependencias no declaradas, violacion del principio de autonomia en la UI, documentacion inconsistente). Cerrar estos items elimina deuda tecnica antes del release v1.0.
Output: Archivos corregidos, test de regresion para CDN, requirements.txt completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tech-debt-cleanup/06-RESEARCH.md

<interfaces>
<!-- Archivos clave que el executor necesita entender -->

From requirements.txt (estado actual — 9 lineas, faltan scipy y numpy):
```
fastapi==0.135.2
uvicorn[standard]==0.42.0
rembg[cpu]==2.0.74
Pillow==12.1.1
typer==0.24.1
Jinja2==3.1.6
PyYAML==6.0.3
watchdog==6.0.0
python-multipart>=0.0.9
```

From app/processor.py linea 115 (patron actual a reemplazar):
```python
transparent_pixels = sum(1 for p in alpha_channel.get_flattened_data() if p < 128)
```

From app/processor.py lineas 176, 183, 244 (imports locales existentes de numpy/scipy):
```python
# linea 176 (dentro de _clean_alpha_artifacts):
import numpy as np
# linea 183 (dentro de _clean_alpha_artifacts):
from scipy import ndimage
# linea 244 (dentro de otra funcion):
import numpy as np
```

From app/templates/ui.html lineas 8-9 (CDN externos a eliminar):
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<script src="https://unpkg.com/lucide@0.468.0/dist/umd/lucide.min.js"></script>
```

From app/templates/ui.html linea 50 (font-family actual):
```css
font-family: 'Inter', system-ui, -apple-system, sans-serif;
```

From app/templates/ui.html linea 1172 (guard de lucide actual):
```javascript
if (typeof lucide !== 'undefined') lucide.createIcons();
```

From CLAUDE.md linea 12 (constraint RAM a actualizar):
```
- **RAM**: ≤ 2 GB para el container — obliga a usar birefnet-lite y max_concurrent=1
```

From CLAUDE.md linea 31 (tabla de stack a actualizar):
```
| rembg | 2.0.74 | Remocion de fondo con IA (birefnet-lite) | ... birefnet-lite ... |
```

Iconos data-lucide usados en ui.html (12 unicos):
settings, loader, cpu, chevron-down, image, square, crop, sliders, layers, save, rotate-ccw, code-2

From tests/test_ui.py fixture (patron para tests async):
```python
@pytest.fixture
async def ui_client():
    with patch("rembg.new_session", return_value=MagicMock()):
        async with app.router.lifespan_context(app):
            async with AsyncClient(
                transport=ASGITransport(app=app),
                base_url="http://test",
            ) as client:
                yield client
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencias, processor numpy y documentacion CLAUDE.md (TECH-DEBT-01, 03, 04)</name>
  <files>requirements.txt, app/processor.py, CLAUDE.md</files>
  <read_first>
    - requirements.txt
    - app/processor.py (completo — ver imports top-level y linea 115)
    - CLAUDE.md (buscar todas las ocurrencias de birefnet-lite)
  </read_first>
  <action>
**TECH-DEBT-01 — requirements.txt:**
Agregar estas dos lineas al final de requirements.txt (antes de cualquier linea en blanco):
```
scipy>=1.16
numpy>=2.3
```
Dejar el resto del archivo intacto. No cambiar versiones de otras dependencias.

**TECH-DEBT-04 — app/processor.py:**
1. Agregar `import numpy as np` al bloque de imports top-level del modulo (despues de `from PIL import ...`, antes de `ImageFile.LOAD_TRUNCATED_IMAGES`). Linea aproximada: entre linea 13 y 15 actuales.
2. Reemplazar la linea 115 actual:
   ```python
   transparent_pixels = sum(1 for p in alpha_channel.get_flattened_data() if p < 128)
   ```
   Por:
   ```python
   transparent_pixels = int((np.array(alpha_channel) < 128).sum())
   ```
3. Eliminar los `import numpy as np` locales dentro de funciones (lineas ~176 y ~244) ya que el import es ahora top-level. Mantener `from scipy import ndimage` como import local en `_clean_alpha_artifacts` (scipy solo se usa ahi).

**TECH-DEBT-03 — CLAUDE.md:**
Actualizar las siguientes lineas (NO tocar la linea 82 sobre GPU future variant ni la linea 71 sobre el anti-pattern de sesion por request):

- Linea 12 — ANTES: `- **RAM**: ≤ 2 GB para el container — obliga a usar birefnet-lite y max_concurrent=1`
  DESPUES: `- **RAM**: ≤ 2 GB para el container — obliga a usar isnet-general-use y max_concurrent=1`

- Linea 31, columna "Purpose" de rembg en la tabla — ANTES contiene `birefnet-lite` dos veces.
  DESPUES: Reemplazar `Remocion de fondo con IA (birefnet-lite)` por `Remocion de fondo con IA (isnet-general-use)`. En la columna "Why Recommended", reemplazar `incluyendo birefnet-lite` por `incluyendo isnet-general-use`.
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && grep -q "scipy>=1.16" requirements.txt && grep -q "numpy>=2.3" requirements.txt && grep -q "^import numpy as np" app/processor.py && grep -q "np.array(alpha_channel)" app/processor.py && ! grep -q "birefnet-lite" CLAUDE.md || grep -c "birefnet-lite" CLAUDE.md | grep -q "^2$" && python3 -m pytest tests/test_processor.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contiene la linea `scipy>=1.16`
    - requirements.txt contiene la linea `numpy>=2.3`
    - app/processor.py tiene `import numpy as np` en el bloque top-level (antes de linea 20)
    - app/processor.py linea 115 (aprox) contiene `int((np.array(alpha_channel) < 128).sum())`
    - app/processor.py NO tiene `import numpy as np` dentro de funciones (solo el top-level)
    - app/processor.py MANTIENE `from scipy import ndimage` dentro de `_clean_alpha_artifacts`
    - CLAUDE.md linea 12 contiene `isnet-general-use` en lugar de `birefnet-lite`
    - CLAUDE.md linea 31 (tabla rembg) contiene `isnet-general-use` en lugar de `birefnet-lite`
    - CLAUDE.md linea 71 (anti-pattern sesion por request) MANTIENE `birefnet-lite` (es un ejemplo)
    - CLAUDE.md linea 82 (GPU variant) MANTIENE `birefnet-lite` (es escenario futuro)
    - `python3 -m pytest tests/test_processor.py -x -q` pasa en verde
  </acceptance_criteria>
  <done>requirements.txt declara scipy y numpy explicitamente. processor.py usa numpy vectorizado para conteo de pixeles. CLAUDE.md documenta isnet-general-use como modelo activo. Tests existentes pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar CDN externos de UI + test de regresion (TECH-DEBT-02)</name>
  <files>app/templates/ui.html, tests/test_ui.py</files>
  <read_first>
    - app/templates/ui.html (completo — ver lineas 8-9 CDN, linea 50 font-family, todos los data-lucide, linea 1172 guard lucide)
    - tests/test_ui.py (completo — ver fixture ui_client y patron de tests)
  </read_first>
  <action>
**Eliminar Google Fonts (linea 8):**
- Eliminar completamente la linea `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">`
- Cambiar en linea 50 el font-family: `font-family: 'Inter', system-ui, -apple-system, sans-serif;` por `font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;`

**Eliminar Lucide CDN (linea 9) y reemplazar con SVGs inline:**
- Eliminar la linea `<script src="https://unpkg.com/lucide@0.468.0/dist/umd/lucide.min.js"></script>`
- Agregar un bloque SVG sprite oculto justo despues del tag `<body>` con los 12 iconos como `<symbol>`. Obtener los SVG paths de lucide@0.468.0 (los paths son del repositorio publico lucide-icons/lucide).

Los 12 simbolos necesarios (todos viewBox="0 0 24 24", fill="none", stroke="currentColor", stroke-width="2", stroke-linecap="round", stroke-linejoin="round"):

1. **settings**: `<circle cx="12" cy="12" r="3"/><path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"/>`
2. **loader**: `<path d="M12 2v4"/><path d="m16.2 7.8 2.9-2.9"/><path d="M18 12h4"/><path d="m16.2 16.2 2.9 2.9"/><path d="M12 18v4"/><path d="m4.9 19.1 2.9-2.9"/><path d="M2 12h4"/><path d="m4.9 4.9 2.9 2.9"/>`
3. **cpu**: `<rect width="16" height="16" x="4" y="4" rx="2"/><rect width="6" height="6" x="9" y="9" rx="1"/><path d="M15 2v2"/><path d="M15 20v2"/><path d="M2 15h2"/><path d="M2 9h2"/><path d="M20 15h2"/><path d="M20 9h2"/><path d="M9 2v2"/><path d="M9 20v2"/>`
4. **chevron-down**: `<path d="m6 9 6 6 6-6"/>`
5. **image**: `<rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/>`
6. **square**: `<rect width="18" height="18" x="3" y="3" rx="2"/>`
7. **crop**: `<path d="M6 2v14a2 2 0 0 0 2 2h14"/><path d="M18 22V8a2 2 0 0 0-2-2H2"/>`
8. **sliders**: `<line x1="4" x2="4" y1="21" y2="14"/><line x1="4" x2="4" y1="10" y2="3"/><line x1="12" x2="12" y1="21" y2="12"/><line x1="12" x2="12" y1="8" y2="3"/><line x1="20" x2="20" y1="21" y2="16"/><line x1="20" x2="20" y1="12" y2="3"/><line x1="2" x2="6" y1="14" y2="14"/><line x1="10" x2="14" y1="8" y2="8"/><line x1="18" x2="22" y1="16" y2="16"/>`
9. **layers**: `<path d="m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z"/><path d="m22.54 12.43-1.42-.65-8.28 3.78a2 2 0 0 1-1.66 0l-8.29-3.78-1.42.65a1 1 0 0 0 0 1.84l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.85Z"/><path d="m22.54 16.43-1.42-.65-8.28 3.78a2 2 0 0 1-1.66 0l-8.29-3.78-1.42.65a1 1 0 0 0 0 1.84l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.85Z"/>`
10. **save**: `<path d="M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z"/><path d="M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7"/><path d="M7 3v4a1 1 0 0 0 1 1h7"/>`
11. **rotate-ccw**: `<path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/><path d="M3 3v5h5"/>`
12. **code-2**: `<path d="m18 16 4-4-4-4"/><path d="m6 8-4 4 4 4"/><path d="m14.5 4-5 16"/>`

- Reemplazar TODOS los `<i data-lucide="NOMBRE" ...></i>` por `<svg class="icon" width="W" height="H"><use href="#icon-NOMBRE"/></svg>`. Respetar los width/height originales de cada uso:
  - `<i data-lucide="settings" width="22" height="22">` -> `<svg class="icon" width="22" height="22"><use href="#icon-settings"/></svg>`
  - `<i data-lucide="loader" width="18" height="18">` -> `<svg class="icon" width="18" height="18"><use href="#icon-loader"/></svg>`
  - Todos los demas usan tamanio implicito -> usar `width="18" height="18"` (tamanio default de Lucide)

- Actualizar los selectores CSS que referencian `i[data-lucide]`:
  - `.page-header i[data-lucide]` (linea 76) -> `.page-header .icon`
  - `.section-title i[data-lucide]` (linea 252) -> `.section-title .icon`
  - `button i[data-lucide]` (linea 458) -> `button .icon`

- Agregar al CSS la clase `.icon` con estilos base: `display: inline-block; vertical-align: middle; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round;`

- Reemplazar el guard de Lucide en linea 1172:
  ANTES: `if (typeof lucide !== 'undefined') lucide.createIcons();`
  DESPUES: eliminar esta linea (los SVGs inline no necesitan inicializacion JS).

**Test de regresion — tests/test_ui.py:**
Agregar despues de `test_ui_no_static_references`:

```python
async def test_ui_no_external_cdn(ui_client):
    """UI-01: HTML autocontenido — sin requests a CDNs externos."""
    import re
    resp = await ui_client.get("/ui")
    html = resp.text
    assert "fonts.googleapis.com" not in html
    assert "unpkg.com" not in html
    # Ningun https:// externo en el HTML
    external_urls = re.findall(r'https?://[^\s"\'<>]+', html)
    assert len(external_urls) == 0, f"External URLs found: {external_urls}"
```
  </action>
  <verify>
    <automated>cd /home/sanchez/proyectos/objetiva-comercios-imgproc && python3 -m pytest tests/test_ui.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/templates/ui.html NO contiene `fonts.googleapis.com`
    - app/templates/ui.html NO contiene `unpkg.com`
    - app/templates/ui.html NO contiene `<script src="https://`
    - app/templates/ui.html NO contiene `<link href="https://`
    - app/templates/ui.html contiene `<svg style="display:none"` (sprite oculto)
    - app/templates/ui.html contiene `<symbol id="icon-settings"` (al menos 1 simbolo)
    - app/templates/ui.html contiene `<use href="#icon-` (al menos 1 uso del sprite)
    - app/templates/ui.html linea 50 font-family es `system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`
    - app/templates/ui.html NO contiene `lucide.createIcons()`
    - app/templates/ui.html contiene clase CSS `.icon`
    - tests/test_ui.py contiene funcion `test_ui_no_external_cdn`
    - `python3 -m pytest tests/test_ui.py -x -q` pasa en verde (todos los tests incluyendo el nuevo)
    - `python3 -m pytest tests/ -x -q` pasa en verde (suite completa no se rompe)
  </acceptance_criteria>
  <done>ui.html es 100% autocontenido sin requests a dominios externos. Los 12 iconos se renderizan via SVG sprite inline. Font usa system stack. Test de regresion previene reintroduccion de CDN.</done>
</task>

</tasks>

<verification>
1. `grep -q "scipy>=1.16" requirements.txt && grep -q "numpy>=2.3" requirements.txt` — dependencias declaradas
2. `grep -c "birefnet-lite" CLAUDE.md` — debe retornar 2 (lineas 71 y 82 que se mantienen)
3. `grep -q "isnet-general-use" CLAUDE.md` — modelo correcto documentado
4. `grep -q "np.array(alpha_channel)" app/processor.py` — numpy vectorizado
5. `! grep -q "fonts.googleapis.com" app/templates/ui.html` — sin Google Fonts CDN
6. `! grep -q "unpkg.com" app/templates/ui.html` — sin Lucide CDN
7. `python3 -m pytest tests/ -x -q` — suite completa en verde
</verification>

<success_criteria>
- Los 4 items de tech debt (TECH-DEBT-01 a 04) estan cerrados
- requirements.txt es completo y correcto para pip install
- La UI funciona sin internet (cero requests externos)
- La documentacion CLAUDE.md es consistente con la implementacion
- processor.py usa numpy para conteo de pixeles (24x mas rapido)
- Test de regresion previene reintroduccion de CDN externos
- Suite completa de tests pasa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-cleanup/06-01-SUMMARY.md`
</output>
